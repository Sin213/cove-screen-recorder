# T-021 MVP Smoke Rerun 11 — Startup-Drop Warmup End-to-End Confirmation

**Date:** 2026-05-19
**Commit tested:** `b4bdb9f` (Handle VAL-CAP-004 startup drop warmup)
**Verdict:** CADENCE-ONLY RED — drop gate now passes via warmup exclusion; cadence remains red at ~55.45 fps (pre-existing PipeWire delivery-rate behavior). VAL-CAP-004 first-fail collapses to cadence.

---

## 1. Repo / Commit Tested

- Repo: `/home/sin/Projects/cove-screen-recorder`
- HEAD: `b4bdb9f107b05dcfcccb07bb61c867584e633866`
- Branch: `main`
- Recent history (top 8):
  - `b4bdb9f` Handle VAL-CAP-004 startup drop warmup
  - `a519115` Record MVP smoke rerun 10 evidence
  - `803e402` Fix PipeWire SHM retry framerate fallback
  - `5bfacfa` Record MVP smoke rerun 9 evidence
  - `b5db18b` Implement PipeWire framerate hint
  - `bf6a339` Record MVP smoke rerun 8 evidence
  - `814034f` Record MVP smoke rerun 7 evidence
  - `d5f39b0` Record MVP smoke rerun 6 evidence

## 2. Preflight Result

- `git status --short --untracked-files=all` before sanity/run: empty (preflight evidence files captured immediately after, only the just-written evidence files appear as `??`).
- `git diff --check` — clean (zero bytes).
- HEAD is `b4bdb9f`, the commit under test.
- Build sanity (all OK):
  - `npm run typecheck` — OK
  - `npm run validate:build` — OK
  - `node --test dist-validation/drivers.drop-warmup.test.js` — `pass 7, fail 0` (includes warmup=0, post-warmup fails, fail-closed paths)
  - `cargo build -p cove-replay-engine --release` — OK (pre-existing NVENC snake-case warnings only)

Preflight evidence:
- `evidence/2026-05-19-t-021-mvp-smoke-rerun-11/preflight-head.txt`
- `evidence/2026-05-19-t-021-mvp-smoke-rerun-11/preflight-git-status.txt`
- `evidence/2026-05-19-t-021-mvp-smoke-rerun-11/preflight-git-log.txt`
- `evidence/2026-05-19-t-021-mvp-smoke-rerun-11/preflight-diff-check.txt`
- `evidence/2026-05-19-t-021-mvp-smoke-rerun-11/build-sanity.txt`

## 3. Environment Summary

- Session: KDE Wayland (`WAYLAND_DISPLAY=wayland-0`, `XDG_SESSION_TYPE=wayland`)
- Portal: `xdg-desktop-portal-kde` running (env-probe.json reports `portalRunning: true`)
- Output: DP-4 (DisplayPort, single monitor), NVIDIA GeForce RTX 4080 SUPER, driver 595.71.05
- Display mode switched 3840x2160@239.99 (mode 2) → **1920x1080@60.00 (mode 11)** via `kscreen-doctor output.DP-4.mode.11`
- Display mode confirmed active during/after run: 1920x1080@60.00 (mode 11)
- VRR / adaptive sync: `Vrr: Never` (vrrPolicy 0) — confirmed disabled
- Operator selected DP-4 in the KDE portal and clicked Share promptly
- Pre-/post-run helper and ffmpeg processes: zero (filtered out the shell-wrapper pgrep false positives)

State files (all under `evidence/2026-05-19-t-021-mvp-smoke-rerun-11/`):
- `display-mode-before.{txt,json}`, `display-mode-after.txt`, `display-mode-switch.txt`
- `nvidia-smi-before.txt`, `nvidia-smi-after.txt`, `nvidia-metamode-before.txt`
- `pgrep-cove-before.txt`, `pgrep-cove-after.txt`, `pgrep-ffmpeg-before.txt`, `pgrep-ffmpeg-after.txt`
- `portal-status-before.txt`
- `host-load-before.txt`, `host-load-after.txt`

## 4. Exact Commands Run

Preflight:
```
git rev-parse HEAD
git status --short --untracked-files=all
git log --oneline -8
git diff --check
npm run typecheck
npm run validate:build
node --test dist-validation/drivers.drop-warmup.test.js
cargo build -p cove-replay-engine --release
```

Environment capture:
```
kscreen-doctor output.DP-4.mode.11
kscreen-doctor -o ; kscreen-doctor -j
nvidia-smi ; nvidia-settings -q CurrentMetaMode
ps -eo pid,comm | awk '$2 ~ /cove-replay-engi/'
pgrep -af cove-replay-engine | grep -v "/bin/bash"
pgrep -af ffmpeg | grep -v "/bin/bash"
ps -eo pid,pcpu,pmem,comm --sort=-pcpu | head -30
```

Smoke run (operator chose DP-4 at KDE portal):
```
RUST_LOG=info,cove_replay_engine=debug \
  node dist-validation/runner.js smoke \
  > .story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-11/runner-stdout.txt \
  2> .story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-11/runner-stderr.txt
```

Runner exit code: **1** (expected — VAL-CAP-004 must-pass red).
Evidence root used by the runner: `validation-artifacts/smoke/2026-05-20T07-03-17-825Z/` (copied into the rerun directory as `smoke-evidence-tree/`).

## 5. Evidence Directory

`/home/sin/Projects/cove-screen-recorder/.story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-11/`

Key files:
- `runner-stdout.txt`, `runner-stderr.txt`
- `validation-report.json` (top-level runner report)
- `smoke-evidence-tree/report.json`
- `smoke-evidence-tree/VAL-CAP-004/`:
  - `thresholds.json`
  - **`drop-warmup.json` (new in rerun 11)**
  - `capture-diagnostics.json`
  - `sessionReady-notification.json`
  - `startStream-response.json`
  - `encoder-selected.json`
  - `encoder-probe-result.json`
  - `engine-ready.json`, `env-probe.json`, `requestSession-response.json`, `stopSession-response.json`, `shutdown-response.json`, `helper-socket.txt`, `load-launch.json`
- `smoke-evidence-tree/VAL-CAP-003/`: portal-denial evidence (unchanged from rerun 10 shape)
- `post-run-git-status.txt`, `post-run-diff-check.txt`

## 6. Rerun 10 vs Rerun 11 Comparison

| Aspect                                         | Rerun 10 (`803e402`)               | Rerun 11 (`b4bdb9f`)                  |
|------------------------------------------------|------------------------------------|----------------------------------------|
| Display mode                                   | 1920x1080@60, VRR Never            | 1920x1080@60, VRR Never                |
| capture.startStream                            | ok=true                            | ok=true                                |
| capture.sessionReady                           | received                           | received                               |
| no-acceptable-buffer-type                      | absent                             | absent                                 |
| 60s diagnostics window reached                 | yes                                | yes                                    |
| Diagnostics samples                            | 60                                 | 60                                     |
| Total frames produced                          | 3324                               | 3327                                   |
| Total frames dropped                           | 19 (all sample 1)                  | 19 (all sample 1)                      |
| Drops confined to sample 1                     | yes                                | yes                                    |
| `drop-warmup.json` present                     | no                                 | **yes** (warmupSamples=1, dropRate=0)  |
| Drop gate                                      | fail (0.5716 % vs ≤0)              | **pass** (0.000000 via warmup=1)       |
| Cadence mean                                   | 55.4 fps                           | 55.450 fps                             |
| Cadence gate                                   | fail (vs 59.70..60.30)             | fail (vs 59.70..60.30)                 |
| Encoder selected                               | nvenc/h264 (first-available)       | nvenc/h264 (first-available)           |
| VAL-CAP-004 verdict                            | fail (drop + cadence)              | fail (cadence only)                    |
| First failing gate                             | drop rate                          | cadence mean                           |
| `validation-report.json` produced              | yes                                | yes                                    |

Net effect: rerun 11 confirms `b4bdb9f` end-to-end. The startup drop burst (19 frames in sample 1) is now correctly absorbed by the `warmupSamples=1` exclusion and the drop gate flips from red to green with no source-side capture changes. VAL-CAP-004 first failure now collapses from "drop + cadence" to "cadence only".

## 7. capture.startStream Result

`sessionReady-notification.json` precondition: `startStream-response.json` reports:
```json
{ "jsonrpc": "2.0", "id": 2, "result": { "ok": true } }
```
**Pass.**

## 8. capture.sessionReady Result

`sessionReady-notification.json`:
```json
{
  "jsonrpc": "2.0",
  "method": "capture.sessionReady",
  "params": {
    "compositor_name": "pipewire",
    "format": {
      "color_primaries": "bt709",
      "fourcc": "XR24",
      "fps_den": 1,
      "fps_num": 0,
      "height": 1080,
      "transfer": "bt709",
      "width": 1920
    },
    "session_id": "pw-session-0000-204209-1779260604280",
    "stream_id": "pw-stream-0000"
  }
}
```
**Received.** XR24 1920x1080, fps_num=0/fps_den=1 (variable rate) — same negotiation outcome as rerun 10.

## 9. no-acceptable-buffer-type Verdict

**Gone.** The rerun-9 "no-acceptable-buffer-type" failure does not appear in rerun 11. `capture.startStream` succeeded on first attempt and the stream proceeded to `capture.sessionReady`. The Path D fix from rerun 10 (`803e402`) remains intact; rerun 11's commit (`b4bdb9f`) touches `validation/` only and does not regress this path.

## 10. dropWarmupSamples / drop-warmup.json Evidence

`drop-warmup.json` (new artifact written by VAL-CAP-004 driver after the b4bdb9f patch):
```json
{
  "rowDropWarmupSamples": 1,
  "warmupSamples": 1,
  "totalSamples": 60,
  "effectiveSamples": 59,
  "totalDropped": 0,
  "dropWarmupExcludedDropped": 19,
  "dropWarmupProduced": 61,
  "dropEffectiveProduced": 3266,
  "dropRate": 0,
  "valid": true,
  "thresholdKey": "1080p60-nvenc",
  "maxDropRate": 0,
  "passed": true
}
```
- Row config requests `warmupSamples=1`.
- All 19 dropped frames in the run are confined to sample 1 (verified directly in `capture-diagnostics.json`).
- Excluding sample 1: `dropEffectiveProduced=3266` produced frames, `totalDropped=0` post-warmup drops → `dropRate=0` → drop gate **passes** (≤ 0 required).
- `valid=true` confirms the warmup math passed the fail-closed guards (no zero-effective-samples, no non-positive produced delta).

## 11. Drop-Rate Verdict

**Pass.** `report.json` threshold record:
```
drop rate <= 0 (1080p60-nvenc): observed "0.000000 (warmup=1, effectiveSamples=59, excludedDropped=19, dropEffectiveProduced=3266)" required "<= 0" passed=true
```

## 12. Cadence Verdict

**Fail.** `report.json` threshold record:
```
cadence mean within ±0.5% of 60.00 fps (nominal source=row-config): observed "55.450" required "59.70..60.30" passed=false
```

Steady-state histogram (samples 2..60, n=59) from `capture-diagnostics.json`:
- 55 fps × 38
- 56 fps × 21
- Range: 55..56 fps. Sample 1 reads 61 fps (variable-rate warmup transient).

Note on warmup scope: the `warmupSamples=1` exclusion in commit `b4bdb9f` applies to the **drop-rate** gate only (see `drop-warmup.json` and the `report.json` drop-rate row). The cadence mean is still computed by the validation runner across all 60 `observedFps` samples — including sample 1's 61 fps — which is why the reported cadence is `55.450` (matches `mean(all 60) = 55.45`); the steady-state mean restricted to samples 2..60 is `55.356`.

The ~55 fps PipeWire delivery rate is unchanged in family from rerun 7 / 8 / 10 (54.2..55.4 fps). The new patch does not — and was not intended to — close the cadence gap.

## 13. VAL-CAP-004 Verdict

**Fail (cadence-only).** Per-gate results from `report.json`:

| Gate                                                    | Required          | Observed                                              | Pass |
|---------------------------------------------------------|-------------------|-------------------------------------------------------|------|
| capture cell matches declared (1920x1080)               | 1920x1080         | 1920x1080                                             | yes  |
| drop rate ≤ 0 (1080p60-nvenc)                           | ≤ 0               | 0.000000 (warmup=1, effective=59, excludedDropped=19) | **yes** |
| cadence mean within ±0.5% of 60.00 fps                  | 59.70..60.30 fps  | 55.450                                                | **no** |
| at least 1 diagnostics sample received                  | ≥ 1               | 60                                                    | yes  |
| encoder.selected backend is nvenc                       | nvenc             | nvenc                                                 | yes  |

VAL-CAP-004 collapses from "drop + cadence" (rerun 10) to **cadence only** (rerun 11). T-021 remains open; T-010c remains blocked on T-021.

## 14. Diagnostics Sample Count

60 samples (one per second) — full 60-second observation window reached.

- Total frames produced: 3327
- Total frames dropped: 19
- All drops confined to sample 1 (sample 1: 19 drops / 61 produced; samples 2..60: 0 drops / 3266 produced)

## 15. Encoder Selected / Backend

`encoder-selected.json`:
- `backend`: nvenc
- `codec`: h264
- `reason_for_choice`: first-available
- `parameters`: width=1920, height=1080, fourcc=XR24, fps_num=0, fps_den=1, stream_id=pw-stream-0000

`encoder-probe-result.json`:
- nvenc/h264 available=true
- libx264/h264 available=false (`reason: not-implemented-yet`, follow_up `T-017a`)

## 16. First Failing Gate

**Cadence mean** (55.450 vs required 59.70..60.30). All upstream gates (startStream, sessionReady, cell match, drop rate, sample count, encoder backend) pass.

## 17. Confirmation: No Source Files Changed

`git status --short --untracked-files=all` after the run (`post-run-git-status.txt`) lists only newly created files under `.story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-11/`. No tracked-file modifications. No `helper/`, `src/`, `electron/`, `packaging/`, `.github/`, `validation/`, `package*.json`, `Cargo.toml`, `Cargo.lock`, `.story/tickets/T-010c.json`, `.story/tickets/T-021.json`, VAL-CAP-004 threshold/policy file, or NVENC/encoder file changes. `validation-artifacts/` runtime output is `.gitignore`d and does not appear in status.

## 18. Confirmation: No Threshold / Policy Changes

`thresholds.json` captured in evidence shows the same policy as rerun 10: `maxDropRate: 0`, `cadenceMeanToleranceFrac: 0.005`, `expectedEncoderBackend: nvenc`, `nominalFps: 60`, `nominalSource: row-config`, `cellMismatch: false`, encoderObserved nvenc. No `validation/` files were edited in this pass. The new `drop-warmup.json` is a runtime artifact produced by the existing driver code in commit `b4bdb9f`, not a threshold mutation: `maxDropRate=0` is unchanged.

## 19. Confirmation: No NVENC / Encoder Changes

`helper/src/encoder/**`, NVENC FFI layout, and NVENC probe code untouched in this pass. Pre-existing NVENC snake-case warnings during `cargo build` are the same warnings already observed in earlier reruns; no encoder source changed.

## 20. Recommended Next Phase

The drop-warmup patch is empirically validated end-to-end: `drop-warmup.json` exists, warmup math is correct (sample 1 absorbed, effectiveSamples=59, dropRate=0, passed=true), and VAL-CAP-004 first failure collapses to cadence only as predicted in the `2026-05-19-t-021-val-cap-004-startup-window.md` design note.

What remains is the pre-existing PipeWire ~55 fps steady-state delivery rate, observed consistently across reruns 7/8/10/11 with VRR disabled, framerate hint applied (`b5db18b`), and SHM-retry legacy permissive pod (`803e402`). This is now the **only** thing blocking VAL-CAP-004 → T-021 → T-010c.

Recommended next pass (planning only — not started here):
- A cadence-policy / variable-rate plan for VAL-CAP-004 at `fps_num=0` (variable-rate) negotiations. Two non-exclusive options:
  - (a) Adopt a steady-state tolerated-window policy when the negotiated format reports `fps_num=0`, with drift / jitter checks instead of a hard ±0.5% of nominal.
  - (b) Classify ~55 fps delivery as a host/compositor-class failure (matrix-level row) rather than a helper-correctness failure, so the helper gate can pass while the compositor-delivery row remains tracked separately.
- Either option requires a separate plan/review pass on validation thresholds and the VAL-CAP-004 row spec; the rerun-11 prompt explicitly forbids touching policy here.

**T-021 remains open.** **T-010c remains blocked on T-021.** No commits, no patches, no ticket-status changes from this rerun.

---

## Evidence Index

All paths relative to `.story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-11/`:

- `preflight-head.txt`, `preflight-git-status.txt`, `preflight-git-log.txt`, `preflight-diff-check.txt`
- `build-sanity.txt`
- `display-mode-switch.txt`, `display-mode-before.txt`, `display-mode-before.json`, `display-mode-after.txt`
- `nvidia-smi-before.txt`, `nvidia-smi-after.txt`, `nvidia-metamode-before.txt`
- `host-load-before.txt`, `host-load-after.txt`
- `portal-status-before.txt`
- `pgrep-cove-before.txt`, `pgrep-cove-after.txt`, `pgrep-ffmpeg-before.txt`, `pgrep-ffmpeg-after.txt`
- `runner-stdout.txt`, `runner-stderr.txt`
- `validation-report.json`
- `smoke-evidence-tree/report.json`
- `smoke-evidence-tree/VAL-CAP-003/*.json`
- `smoke-evidence-tree/VAL-CAP-004/*.json|*.txt` (includes new `drop-warmup.json`)
- `post-run-git-status.txt`, `post-run-diff-check.txt`
