# T-021 Rerun 5 — MVP Smoke Validation (RED)

**Date:** 2026-05-19
**Pass id:** T-021 rerun 5
**Repo path:** /home/sin/Projects/cove-screen-recorder
**Branch:** main
**Commit tested:** `8fe6aef Record MVP smoke rerun 4 evidence`

---

## 1. Preflight

| Check | Result |
|---|---|
| `git status --short` | Clean — no changes |
| Latest commit | `8fe6aef Record MVP smoke rerun 4 evidence` ✓ |
| `git diff --check` | Clean |
| ISS-001 | resolved (unchanged) |
| ISS-002 | was resolved (T-024) — **REOPENED by this pass** (nvenc-api-create-failed:15) |
| ISS-003 | was open — **RESOLVED by this pass** (cell-match gate confirmed) |

## 2. Build / Test Results

Builds were validated in rerun 4. No source file changed between rerun 4 and rerun 5. Build state:

| Command | Result (from rerun 4, unchanged) |
|---|---|
| `cargo build -p cove-replay-engine --release` | `Finished release` — clean |
| `cargo test -p cove-replay-engine` | 139 passed, 0 failed |
| `npm run typecheck` | Clean |
| `npm run validate:build` | Clean |
| `npm run build` | Clean |

## 3. Environment Probe

| Variable | Value |
|---|---|
| `XDG_RUNTIME_DIR` | `/run/user/1000` |
| `DBUS_SESSION_BUS_ADDRESS` | `unix:path=/run/user/1000/bus` |
| `DISPLAY` | `:1` |
| `WAYLAND_DISPLAY` | `wayland-0` |
| `xdg-desktop-portal` | running (PID 1920) |
| `xdg-desktop-portal-gtk` | running (PID 2253) |
| `xdg-desktop-portal-kde` | running (PID 2475) |
| `cove-replay-engine` (pre-run) | not running |
| canonical socket | not present |
| GPU | NVIDIA GeForce RTX 4080 SUPER, driver 595.71.05 |

**Rerun 4 note:** Rerun 4 timed out at `capture.requestSession` because the KDE portal picker was hidden behind the kiosk Chromium browser (`--kiosk` fullscreen). Rerun 5 was executed from a visible desktop terminal (Konsole) with the operator present and minimizing the kiosk browser to expose and approve the portal dialog. The operator selected a monitor and clicked Share.

## 4. Runner Command

```
node dist-validation/runner.js smoke
```

(Desktop terminal, operator-interactive. VAL-CAP-004 self-spawns its own helper via `runnerOwnedSocketPath()`.)

## 5. Suite Result

```
Suite: smoke  |  Verdict: FAIL  |  2026-05-19T06:59:35.326Z
  pass=1  fail=1  skip=2  error=0  stoppedEarly=true  stoppedAtRow=VAL-CAP-004
```

Started: 2026-05-19T06:59:35.326Z
Completed: 2026-05-19T07:00:48.842Z

## 6. Row-by-Row Table

| ID | Status | Class | Reason / Message |
|---|---|---|---|
| VAL-PKG-001 | skip | scripted-local | helper-not-available (canonical socket absent) |
| VAL-CAP-001 | skip | manual | manual |
| VAL-CAP-003 | **pass** | scripted-local | Portal denial emitted capture.sessionLost(portal-denied); helper returned to IDLE |
| **VAL-CAP-004** | **FAIL** | scripted-local | Capture quality gates failed — see thresholds (key=1080p60-nvenc, samples=62) |
| VAL-CAP-006 … VAL-REG-002 | — | — | Suite stopped early at VAL-CAP-004 (first must-pass red) |

## 7. VAL-CAP-004 Detail

### Helper lifecycle

- Self-spawned via `runnerOwnedSocketPath()` (VAL-CAP-004 in `SELF_SPAWNING_ROW_IDS`)
- Socket: `/run/user/1000/cove-screen-recorder/runner-32642ae5.sock`
- Load: `bash validation/loads/l-motion-60/launch.sh` (PID 613243)
- Helper: PID 613511 — `engine.ready` received
- Portal: operator minimized kiosk Chromium, approved KDE "Screen Sharing" dialog
- `capture.requestSession` → ok=true
- `capture.startStream` → ok=true
- `capture.sessionReady` received (see below)
- `capture.stopSession` → ok=true
- `engine.shutdown` → ok=true

### sessionReady — negotiated capture format

```json
{
  "compositor_name": "pipewire",
  "format": {
    "color_primaries": "bt709",
    "fourcc": "XR24",
    "fps_den": 1,
    "fps_num": 0,
    "height": 2160,
    "width": 3840
  },
  "session_id": "pw-session-0000-613511-1779173985566",
  "stream_id": "pw-stream-0000"
}
```

Negotiated: **3840×2160, variable-rate (fps_num=0)** — 4K monitor, same as prior runs.

### encoder.probeResult

```json
{
  "backends": [
    {
      "available": false,
      "backend": "nvenc",
      "codec": "h264",
      "details": { "reason": "nvenc-api-create-failed:15" }
    },
    {
      "available": false,
      "backend": "libx264",
      "codec": "h264",
      "details": { "reason": "not-implemented-yet", "follow_up_ticket": "T-017a" }
    }
  ]
}
```

**NVENC probe FAILED** at runtime: `NvEncodeAPICreateInstance` returned status code 15. T-024 built and unit-tested the NVENC backend, but the runtime probe fails on this hardware/driver configuration. `encoder.selected` was never emitted.

### thresholds.json (key fields)

| Field | Value |
|---|---|
| `key` | `1080p60-nvenc` (declared cell, not negotiated — D3 correct) |
| `maxDropRate` | 0 |
| `cadenceMeanToleranceFrac` | 0.005 |
| `captureFormat` (negotiated) | 3840×2160, fps_num=0 |
| `declaredCaptureFormat` | 1920×1080 |
| `negotiatedCaptureFormat` | 3840×2160 |
| `cellMismatch` | true |
| `expectedEncoderBackend` | nvenc |
| `encoderObserved` | (not received) |
| `nominalFps` | 60 |
| `nominalSource` | row-config |

### Threshold results (in order)

| Gate | Observed | Required | Passed |
|---|---|---|---|
| capture cell matches declared (1920x1080) | 3840x2160 | 1920x1080 | **false** ← T-023 gate |
| drop rate <= 0 (1080p60-nvenc) | 0.002767 | <= 0 | **false** |
| cadence mean within ±0.5% of 60.00 fps (nominal source=row-config) | 104.935 | 59.70..60.30 | **false** |
| at least 1 diagnostics sample received | 62 | >= 1 | true |
| encoder.selected backend is nvenc | (not received) | nvenc | **false** |

### capture-diagnostics summary

62 samples received. observedFps range: 103–107fps (mean ~104.9fps). First sample droppedSinceLast=18 (startup burst), remaining samples clean. This is consistent with a 4K monitor running at its native refresh (~107Hz) — not the declared 1080p60.

### Failure classification

| Failure | Root cause | Blocker layer |
|---|---|---|
| Cell mismatch (primary) | Portal delivered 4K (3840×2160) to the 1080p60-declared row | Product/host: no 1080p source available; T-023 gate fires as designed |
| Drop rate fail | 0.002767 drop rate vs 0 budget — consequence of 4K source against 1080p60 threshold key | Consequential — blocked by cell mismatch |
| Cadence fail | 104.935fps observed vs 60fps declared — 4K monitor at native refresh, nominalFps=60 from row-config (fps_num=0) | Consequential — blocked by cell mismatch |
| encoder.selected not received | NVENC probe failed: `nvenc-api-create-failed:15` | Product: NVENC runtime probe fails on this hardware/driver; **ISS-002 reopened** |

## 8. Helper Lifecycle

- Runner-owned helper lifecycle: self-spawned by driver
- Socket: runner-owned (not canonical)
- Teardown: orderly — stopSession, shutdown, processes exited cleanly
- No pre-existing helper or stale socket at run start

## 9. Portal / Operator Behavior

The KDE ScreenCast portal picker dialog appeared after the kiosk Chromium browser was minimized. Operator selected the intended screen/output and clicked "Share." The `capture.requestSession` call succeeded (ok=true). The negotiated source was the 4K primary monitor (3840×2160).

A restore token existed at `~/.local/state/cove-screen-recorder/portal-restore.json` from a prior run. The helper loaded it automatically, which may have pre-selected the 4K monitor without requiring re-selection of the source type. The `start()` call still required operator approval.

## 10. Verdict

**RED**

**Primary failure:** `capture cell matches declared (1920x1080)` → observed `3840x2160` (first must-pass gate, T-023 cell-match).

This is the expected T-023/ISS-003 result: the portal delivers a 4K source on this hardware, the declared row expects 1080p60. The cell-match gate fires precisely as designed. The row's `onCellMismatch="fail"` policy is correct — this is not a harness defect.

**Secondary failures** (all consequential of 4K source):
- Drop rate and cadence gates fail because the 4K source at ~107fps does not satisfy 1080p60 thresholds
- `encoder.selected` not received because NVENC probe fails at runtime (`nvenc-api-create-failed:15`)

## 11. Ready for T-010c?

**NO.**

T-010c requires a green T-021 verdict. This pass is RED. T-010c remains blocked.

Next steps before T-010c:
1. Resolve NVENC runtime probe failure (ISS-002 now reopened: `nvenc-api-create-failed:15`)
2. Decide matrix-gate policy: either connect a 1080p60-capable source OR switch `onCellMismatch` to `"skip"` for 4K-only hosts (requires a separate policy decision pass)

## 12. Issues Updated

| Issue | Action | Rationale |
|---|---|---|
| ISS-001 | unchanged — resolved | unrelated |
| ISS-002 | **REOPENED** | NVENC probe fails at runtime: `nvenc-api-create-failed:15`. T-024 built the backend but the probe does not succeed on this hardware/driver at runtime. `encoder.selected` was not emitted. |
| ISS-003 | **RESOLVED** | T-023 cell-match gate fired precisely: observed=3840x2160, required=1920x1080, passed=false. ThresholdResult is explicit. Evidence confirms D3 implementation works as designed. |

## 13. Ticket Status After This Pass

| Ticket | Status | Action |
|---|---|---|
| T-021 | open | RED verdict — leave open |
| T-010a | open | T-021 still open — leave open |
| T-010c | blocked | unchanged — do not start |
| T-023 | **complete** | D3 implementation confirmed by runtime evidence |
| T-024 | open | NVENC backend built, but probe fails at runtime — ISS-002 reopened |

## 14. Evidence Directory

`.story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-5/`

| File | Description |
|---|---|
| `preflight.txt` | git status, log, issue/ticket state at session start |
| `env-probe.txt` | environment variable and process scan at session start |
| `smoke-report.json` | copy of smoke report from validation-artifacts/smoke/2026-05-19T06-59-35-326Z/report.json (canonical tracked copy) |
| `smoke-run/2026-05-19T06-59-35-326Z/` | full evidence run directory mirrored from validation-artifacts/smoke/ |
| ↳ `VAL-CAP-004/env-probe.json` | stage 1 env probe |
| ↳ `VAL-CAP-004/load-launch.json` | L-MOTION-60 load PID |
| ↳ `VAL-CAP-004/helper-socket.txt` | runner-owned socket path |
| ↳ `VAL-CAP-004/engine-ready.json` | helper engine.ready notification |
| ↳ `VAL-CAP-004/requestSession-response.json` | capture.requestSession ok=true |
| ↳ `VAL-CAP-004/startStream-response.json` | capture.startStream ok=true |
| ↳ `VAL-CAP-004/sessionReady-notification.json` | sessionReady with 3840×2160 negotiated format |
| ↳ `VAL-CAP-004/encoder-probe-result.json` | nvenc unavailable: nvenc-api-create-failed:15 |
| ↳ `VAL-CAP-004/capture-diagnostics.json` | 62 samples, ~105fps |
| ↳ `VAL-CAP-004/thresholds.json` | full threshold evidence with declared vs negotiated cell |
| ↳ `VAL-CAP-004/stopSession-response.json` | ok=true |
| ↳ `VAL-CAP-004/shutdown-response.json` | ok=true |

## 15. Commands Run

```
git status --short --untracked-files=all          (preflight)
git log --oneline -10                              (preflight)
git diff --check                                   (preflight)
pgrep -af xdg-desktop-portal                       (env probe)
ss -lxp | grep engine.sock                         (env probe)
node dist-validation/runner.js smoke               (smoke run — operator-interactive)
git status --short --untracked-files=all          (post-run)
```

## 16. Source Files Changed

**None.** No file under `helper/`, `validation/`, `electron/`, `src/`, `packaging/`, `.github/workflows/`, `package.json`, `package-lock.json`, `Cargo.toml`, `Cargo.lock` was modified in this pass.

## 17. Prior Handovers Edited

**None.** Prior handovers are preserved verbatim.

## 18. T-010c Execution

**Did not start T-010c.** RED verdict leaves T-010c blocked.

## 19. What Must Change for T-021 to Go Green

Two independent blockers must both be resolved:

**Blocker 1 — NVENC runtime probe failure (ISS-002):**

`encoder.probeResult` shows `nvenc-api-create-failed:15`. `NvEncodeAPICreateInstance` returned status 15 on the RTX 4080 SUPER with driver 595.71.05. The T-024 unit tests passed `nvenc_probe_available_when_hardware_present` — investigate whether the test used a different code path or library loading order than the runtime probe.

**Blocker 2 — 4K source vs 1080p60 declared (ISS-003 / matrix-gate policy):**

The portal always delivers the 4K primary monitor on this hardware. Options:
- Connect a 1080p60 secondary output (physical or virtual) and re-run
- Set `onCellMismatch: "skip"` for 4K-only hosts with the `host-does-not-deliver-declared-cell` skip path (requires a policy decision pass)
- Accept that VAL-CAP-004 is not runnable on a 4K-only host and mark it as a matrix-gate coverage gap

Both blockers require separate passes. Do not patch in this tab.
