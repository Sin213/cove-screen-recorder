# T-044 — ISS-015 Post-Fix Validation: Branch A Confirmed

**Date:** 2026-05-24
**Ticket:** T-044 (complete). **Issues:** ISS-015 (resolved). **Prior:** T-043 (complete).

## Session Goal

Validate T-043's overlap-safe eviction fix against a real replay export flow. Classify result
per decision tree (Branch A–D).

## Result: COMPLETE — Branch A Confirmed

**ISS-015 is resolved.** 2/2 replay saves reached EXPORTING and completed successfully under
4K/XR24/static-screen/dedup conditions. No "committed ring empty" failure.

## Evidence

**Evidence root:** `.story/handovers/evidence/2026-05-24-iss-015-post-fix-validation/`

- `operator-note.md` — full timeline, eviction table, ffprobe results, prior-failure comparison
- `helper-log-extract.jsonl` — engine eviction events (16 lines)
- `ffprobe-export-1.json` — exp-1779658135736303403-0001.mp4
- `ffprobe-export-2.json` — exp-1779658214667035343-0003.mp4

## Session Details

**Helper binary:** target/debug/cove-replay-engine (T-043 overlap fix)
**Session:** pw-session-0000-1855423-1779657979540
**Source:** Monitor, Wayland portal, XR24 SHM fallback, 4K (3840×2160)

## Branch A Classification

All conditions met:

| Condition | Result |
|-----------|--------|
| Save reaches EXPORTING | ✓ (2/2) |
| Committed ring retained at save time | ✓ committed_len_after=1 on all evictions |
| Export completes successfully | ✓ (2/2, 100%) |
| No T-040 "committed ring empty" warning | ✓ never fired |
| Valid MP4 output | ✓ h264, 3840×2160, ~38-39s, 120 frames |

## What Changed vs T-042

Under old predicate (T-042 repro): `committed_len_after=0` → ring always empty at save time.
Under overlap predicate (T-043): `committed_len_after=1` → newest segment always retained.

Eviction still fires every ~39s (correct — old segment ends before cutoff). The in-progress
newest segment's pts_end is always within the window and is never evicted. pin_snapshot
succeeds on every save attempt.

## ISS-012 Status

ISS-012 (stuck EXPORTING) did NOT reproduce during this validation. Both exports transitioned
cleanly. ISS-012 remains open for separate investigation when the user is ready to resume.

## Export Outputs

| Export | Duration | Size | Codec | Resolution |
|--------|----------|------|-------|------------|
| exp-...-0001.mp4 | 38.72s | 11.1MB | h264 | 3840×2160 |
| exp-...-0003.mp4 | 39.28s | 12.8MB | h264 | 3840×2160 |

## Verification

```
git diff --stat
# only .story/** + helper/src/segment/buffer.rs

git diff --name-only
# confirm no other source files
```

Engine log: no `pin_snapshot: committed ring empty` warnings after line 4590.
Renderer log: SAVING → EXPORTING → RECORDING on both attempts (never SAVING → RECORDING).

## Commit Status

NOT committed (per task instructions).

## Tickets/Issues Changed

- T-043: complete (overlap fix implemented)
- T-044: complete (validation passed)
- ISS-015: resolved
