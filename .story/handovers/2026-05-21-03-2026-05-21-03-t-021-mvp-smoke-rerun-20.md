# T-021 MVP Smoke Rerun 20

**Date:** 2026-05-21
**Pass ID:** T-021 MVP smoke rerun 20
**Purpose:** Verify fMP4 trun/moof fix resolves VAL-EXP-001. No source changes during rerun.

---

## 1. Repo / Commit Tested

- **Repo:** /home/sin/Projects/cove-screen-recorder
- **HEAD:** `2acefc3f1bdab293780df4501219d91bc336466d`
- **Commit message:** Fix fMP4 trun layout for exports
- **Branch:** main
- **Working tree:** clean before and after rerun

Recent history:
```
2acefc3 Fix fMP4 trun layout for exports
fea7e34 Record MVP smoke rerun 19 evidence
520e75f Persist init segment before export
6dde09b Record VAL-EXP-001 diagnostic evidence
0961e26 Capture ffmpeg export diagnostics
```

---

## 2. Preflight Result

- `git status --short --untracked-files=all`: CLEAN
- `git diff --check`: CLEAN
- `git log --oneline -10`: matches expected, HEAD = `2acefc3 Fix fMP4 trun layout for exports`
- No stale cove-replay-engine or ffmpeg processes

---

## 3. Display / Environment

- OS: Linux 6.18.32-1-lts
- GPU: NVIDIA GeForce RTX 4080 SUPER, driver 595.71.05
- Display before: DP-4 mode 2 (3840x2160@239.99) — incorrect for smoke
- Action: `kscreen-doctor output.DP-4.mode.11`
- Display after: DP-4 mode 11 (1920x1080@60.00*) confirmed
- VRR: `vrrPolicy: 0` = Never
- Portal: running, Share clicked on DP-4

---

## 4. Verification Sanity Results

```
cargo test -p cove-replay-engine --lib encoder::backends::nvenc::fmp4 → 5 passed (3 new trun/moof tests + 2 existing)
cargo test -p cove-replay-engine --test encoder_session               → 28 passed
cargo test -p cove-replay-engine --test segment_buffer                → 7 passed
cargo build -p cove-replay-engine                                     → 0 errors, 70 pre-existing warnings
npm run typecheck                                                     → PASS
npm run validate:build                                                → PASS
npm run build                                                         → PASS
git diff --check                                                      → PASS
```

---

## 5. Evidence Directory

`.story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-20/`

Contents:
- `runner-stdout.txt`, `runner-stderr.txt`
- `preflight-kscreen-o.txt`, `preflight-kscreen-j.json`
- `preflight-nvidia-smi.txt`, `preflight-pgrep-engine.txt`, `preflight-pgrep-ffmpeg.txt`
- `preflight-sanity.txt`, `preflight-uptime.txt`
- `post-kscreen-o.txt`, `post-kscreen-j.json`, `post-nvidia-smi.txt`
- `post-pgrep-engine.txt`, `post-pgrep-ffmpeg.txt`, `post-uptime.txt`
- `smoke-evidence-tree/2026-05-21T18-24-33-422Z/` (full per-gate artifacts)

---

## 6. Gate Summary

| Gate | Status | Note |
|------|--------|------|
| VAL-PKG-001 | skip | helper-not-available |
| VAL-CAP-001 | skip | manual |
| VAL-CAP-003 | pass | Portal denial handled |
| VAL-CAP-004 | **GREEN** | 1080p60-nvenc, 0 drops, cadence 53.746fps |
| VAL-CAP-006 | skip | manual |
| VAL-UI-005 | skip | manual |
| VAL-ENC-001 | pass | NVENC available |
| VAL-ENC-006 | skip | manual |
| VAL-SEG-001 | pass | Disk budget OK |
| VAL-SEG-003 | **GREEN** | 1 ms latency, 15 segments, IDRs confirmed |
| VAL-EXP-001 | **FAIL** | Exit 234; NEW error: output format .tmp |
| VAL-EXP-010 | not reached | runner stopped early at VAL-EXP-001 |
| VAL-REG-002 | not reached | runner stopped early at VAL-EXP-001 |

---

## 7. VAL-CAP-004 Verdict

**GREEN — PASS**

```
Monitor capture passed drop+cadence gates (60 samples, key=1080p60-nvenc)
```

Thresholds:
- `thresholdKey`: 1080p60-nvenc
- `cellMismatch`: false (1920x1080 ✓)
- `encoderObserved`: nvenc ✓
- Drop rate: 0.0 ✓
- Cadence mean: 53.746 fps (range 51.00..61.20) ✓
- Cadence spread: 2.000 fps (≤6.0) ✓

---

## 8. VAL-SEG-003 Verdict

**GREEN — PASS**

```
replay.save completed in 1 ms (gate: 2250 ms)
```

From VAL-SEG-003 save-response:
- `init_segment_bytes`: **664** ✓
- `init_segment_path`: `/run/user/1000/cove-screen-recorder/segments/pw-session-0000-607862-1779388033780/init.mp4`
- `segments count`: 15
- `has_discontinuity`: false
- `codec`: h264
- `height × width`: 1080 × 1920

---

## 9. init.mp4 Verdict

**EXISTS — PASS**

From `VAL-EXP-001/ffmpeg-diagnostics.txt`:
```
input 0: .../init.mp4 | exists=true | size_bytes=664
```

- `init.mp4` exists: **true**
- `init.mp4` size: **664 bytes**
- init_segment_bytes (save-response): **664**

---

## 10. VAL-EXP-001 Verdict

**FAIL — NEW error (changed from rerun 19)**

Exit code: **234** (was 183 in rerun 19, 254 in rerun 18)
Stage: copy
Reason code: ffmpeg-nonzero-exit

From `VAL-EXP-001/export-terminal-event.json`:
```json
{
  "method": "export.failed",
  "params": {
    "details": "exit code: Some(234)",
    "export_id": "exp-1779388129182033489-0001",
    "reason_code": "ffmpeg-nonzero-exit",
    "stage": "copy"
  }
}
```

---

## 11. ffmpeg Diagnostics (Rerun 20)

**Critical: ffmpeg now reads the input successfully.** This is the key behavioral change from rerun 19.

```
Input #0, mov,mp4,m4a,3gp,3g2,mj2, from 'concat://.../init.mp4|...00001736.mp4|...':
  Metadata:
    major_brand: isom
  Duration: 00:58:22.00, start: 3472.000000, bitrate: 0 kb/s
  Stream #0:0[0x1](und): Video: h264 (High) (avc1), yuv420p(progressive), 1920x1080, 109 kb/s, 1 fps, 1 tbr, 90k tbn
```

ffmpeg then fails at output:
```
[AVFormatContext] Unable to choose an output format for '...exp-...0001.tmp'; use a standard extension for the filename or specify the format manually.
[out#0] Error initializing the muxer: Invalid argument
Error opening output file ...exp-...0001.tmp.
Error opening output files: Invalid argument
```

**All 16 inputs exist and are non-zero:**
- input 0: init.mp4 | exists=true | size_bytes=664
- inputs 1–15: 00001736.mp4–00001750.mp4 | exists=true | ~25–31 KB each

---

## 12. Trun/Moof Fix Assessment

**The patch IS correct and DID resolve the original defect.**

| Error | Rerun 18 | Rerun 19 | Rerun 20 |
|-------|----------|----------|----------|
| init.mp4 present | false | true | true |
| ffmpeg reads input | NO (exit 254, file not found) | NO (exit 183, AVERROR_INVALIDDATA) | **YES** |
| Container detected | — | — | **mov,mp4,m4a,3gp,3g2,mj2** |
| Codec detected | — | — | **h264 (High), 1920x1080** |
| Failure stage | input open | input parse | **output muxer init** |
| Exit code | 254 | 183 | **234** |
| ffmpeg error | No such file or directory | Invalid data found when processing input | **Unable to choose output format for .tmp** |

Specifically:
- "Invalid data found when processing input" → **RESOLVED** ✓
- "trun parse failure" → **RESOLVED** ✓  
- "NAL unit size corruption" → **RESOLVED** ✓ (h264 stream parsed cleanly)

The remaining failure is a new, unrelated issue: the output path uses `.tmp` extension, which ffmpeg cannot use to infer the muxer format. ffmpeg requires either a known extension (`.mp4`) or an explicit `-f mp4` format flag.

---

## 13. Output File / SHA Verdict

- export.completed: NOT present
- export.failed: YES
- output file: NOT found (fileSizeBytes=0 in sha256-check)
- SHA validation: not computed (export failed before producing output)
- ffprobe readability: N/A (no output file)

---

## 14. Can ISS-006 Close?

**YES — the specific defect ISS-006 tracks is resolved.**

ISS-006 = "VAL-EXP-001: malformed fMP4 trun flags and data_offset cause ffmpeg failure"

The trun flags fix (`0x0F0100` → `0x000705`) and moof size fix (100 → 104) are confirmed working: ffmpeg now parses the fMP4 stream without error. The "Invalid data found when processing input" / "trun parse failure" failure mode is gone.

The remaining VAL-EXP-001 failure is a **different root cause**: output file extension `.tmp` prevents ffmpeg from selecting an output muxer. This should be a new issue.

Recommendation: **Close ISS-006 with resolution "trun/moof fix verified working, output format is separate issue". File new issue: "VAL-EXP-001: ffmpeg output path .tmp extension causes muxer init failure".**

---

## 15. Can T-021 Go Green?

**No.** VAL-EXP-001 is a must-pass scripted-local gate and is still red. T-021 cannot green until VAL-EXP-001 passes.

---

## 16. Is T-010c Unblocked?

**No.** T-010c is gated on T-021 green.

---

## 17. First Failing Gate

**VAL-EXP-001** — runner stopped early here. VAL-EXP-010 and VAL-REG-002 were not reached.

---

## 18. Source / Validation / Policy Confirmations

- No source files changed during rerun 20
- No validation logic changed
- No thresholds changed
- No ffmpeg argv changed (the .tmp output path issue pre-exists this rerun)
- No segment-commit predicate changed
- No replay.save behavior changed
- No capture / cadence / NVENC changes
- Only operator action: `kscreen-doctor output.DP-4.mode.11` to satisfy display preflight

---

## 19. Recommended Next Phase

The trun/moof fix is confirmed correct. The next pass must fix the output format issue:

1. In `helper/src/export/mod.rs`, add `-f mp4` to the ffmpeg argv BEFORE the output path (or change the tmp extension to `.tmp.mp4` and strip the `.mp4` on rename), so ffmpeg knows to mux as MP4.
2. Re-run smoke (rerun 21). Expect VAL-EXP-001 to either green or fail with a new error.
3. Only after VAL-EXP-001 greens: green T-021 and unblock T-010c.

Do not start T-010c. Do not green T-021.
