# T-021 MVP Smoke Rerun 25 — Variable-rate spread policy verification

- Date (local): 2026-05-21 15:53–16:02
- Repo: /home/sin/Projects/cove-screen-recorder
- Commit tested: `6f9569cb1adf48b377a0a1480ea3714d0b1ea3b8`
- Pass id: T-021 rerun 25 / variable-rate spread policy verification
- Evidence dir: `.story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-25/`
- Runner artifact: `validation-artifacts/smoke/2026-05-21T22-53-38-426Z/`

## Scope confirmation

Evidence/verification pass only.

- No source files touched (`helper/**`, `electron/**`, `src/**` clean)
- No validation logic touched (`validation/**` clean)
- No thresholds changed (`thresholds.json` reflects committed values: `variableRateCadenceMaxSpreadFps = 20`, etc.)
- No `package.json` / `package-lock.json` / `Cargo.toml` / `Cargo.lock` touched
- No ticket / issue files touched (`.story/tickets/T-021.json`, `.story/tickets/T-010c.json`, `.story/issues/**`)
- Only allowed writes: `.story/handovers/2026-05-21-08-t-021-mvp-smoke-rerun-25.md`, `.story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-25/**`, `/home/sin/Desktop/Claude-Handoff/handoff.md`

## Required-history confirmation

- `Relax variable-rate capture spread threshold` — present (6f9569c)
- `Record MVP smoke rerun 24 evidence` — present (13712e2)
- `Record MVP smoke rerun 23 evidence` — present (f352ccc)
- `Record MVP smoke rerun 22 evidence` — present (f10bd61)
- `Use real capture timestamps for NVENC fragments` — present (be2b653)

## Build / test sanity (all green)

| Command | Result |
|---|---|
| `cargo build -p cove-replay-engine --release` | 0 errors, 70 pre-existing warnings |
| `cargo test -p cove-replay-engine --lib` | 101 passed |
| `cargo test -p cove-replay-engine --test encoder_session` | 28 passed |
| `cargo test -p cove-replay-engine --test segment_buffer` | 7 passed |
| `npm run typecheck` | pass |
| `npm run validate:build` | pass |
| `npm run build` | pass (vite renderer + tsc electron) |

## Display / environment

- DP-4 before mode set: 3840x2160@239.99 (mode 2). Pre-evidence: `kscreen-doctor-pre.{txt,json}`.
- Action: `kscreen-doctor output.DP-4.mode.1920x1080@60`.
- DP-4 at run time: 1920x1080@60.00 (mode 11), VRR `Never`. Confirmation snapshot: `kscreen-doctor-applied.{txt,json}`.
- GPU: NVIDIA RTX 4080 SUPER, driver 595.71.05.
- `pgrep-cove-{pre,post}.txt` and `pgrep-ffmpeg-{pre,post}.txt` contain only the false-positive grep hit for the bash command itself (no real cove or ffmpeg processes lingering pre- or post-run).
- `uptime` pre / post captured.

## Suite verdict

| Metric | Value |
|---|---|
| Verdict | **FAIL** |
| pass / fail / skip / error | 6 / 1 / 5 / 0 |
| Stopped early at | **VAL-EXP-010** |
| First failing gate | **VAL-EXP-010** |

## Gate verdicts

| Row | Status | Notes |
|---|---|---|
| VAL-PKG-001 | skip | `helper-not-available` (runner spawns helper per row; same as prior reruns) |
| VAL-CAP-001 | skip | `manual` |
| VAL-CAP-003 | pass | Portal denial emitted `capture.sessionLost(portal-denied)`; helper to IDLE in 156 ms |
| **VAL-CAP-004** | **pass** | Monitor capture passed drop+cadence gates (60 samples, key=1080p60-nvenc) — see below |
| VAL-CAP-006 | skip | `manual` |
| VAL-UI-005 | skip | `manual` |
| VAL-ENC-001 | pass | NVENC probe confirmed available via `encoder.probeResult` |
| VAL-ENC-006 | skip | `manual` |
| VAL-SEG-001 | pass | Segment buffer stayed within disk budget over 64 samples (max 0.19%) |
| VAL-SEG-003 | pass | `replay.save` completed in 1 ms (gate 2250 ms) |
| **VAL-EXP-001** | **pass** | Export completed: 8 131 951 bytes, SHA256 verified, 60 s window (28.61 s actual) |
| **VAL-EXP-010** | **fail** | PTS / cadence assertion failed — first must-pass red |
| VAL-REG-002 | not reached | runner stopped at VAL-EXP-010 |

## VAL-CAP-004 — mean / spread / drop-rate

(`thresholds.json` key `1080p60-nvenc`, variable-rate policy, `variableRateCadenceMaxSpreadFps = 20`.)

| Threshold | Observed | Required | Pass |
|---|---|---|---|
| capture cell matches declared (1920x1080) | 1920x1080 | 1920x1080 | ✓ |
| drop rate ≤ 0 | `0.000000` (warmup=1, effectiveSamples=59, excludedDropped=19, dropEffectiveProduced=3240) | ≤ 0 | ✓ |
| cadence mean in variable-rate range [51.00..61.20] fps | **54.915** | 51.00..61.20 | ✓ |
| cadence spread ≤ 20.0 fps (variable-rate compositor variance) | **2.000** | ≤ 20.0 | ✓ |
| cadence mean within ±0.5% of 60.00 fps (informational only) | 54.915 | 59.70..60.30 | ✗ (non-gating) |
| at least 1 diagnostics sample received | 60 | ≥ 1 | ✓ |
| encoder.selected backend is nvenc | nvenc | nvenc | ✓ |

**Result:** Variable-rate spread relaxation (commit `6f9569c`, `variableRateCadenceMaxSpreadFps = 20`) holds. Observed spread 2.0 fps fits well inside the new envelope; the rerun-22 / rerun-24 cadence-spread failures (12 fps / 15 fps) would also have passed under this threshold. Drops are 0 on the gating window. Cadence mean is sustained sub-nominal (~55 fps) on the 60 Hz monitor — same compositor pattern as rerun-24, but now within policy.

## VAL-SEG-003 — verdict

Pass. `replay.save` latency 1 ms ≪ 2250 ms gate. `replay.snapshotPinned` received. Capture diagnostics continued during/after save. `active_snapshots` returned to 0 after release.

## VAL-EXP-001 — verdict

Pass.

- terminal event: `export.completed`
- output file: 8 131 951 bytes
- SHA256: `c28f222c93b922629cf94e9596b5263955627bfe99672f54c13a830ac7f132b8` — helper and runner match
- `mode = stream-copy`, `duration_s = 28.6146` (informational duration check fails at ±16.7 ms of 60 s, marked non-gating in row config)
- `final_path = /tmp/cove-val-exp001-1779404274003-4ff6d61ba397c834/export.mp4` (purged after the row completed; SHA256 captured before purge)
- no `cove-replay-engine` or `ffmpeg` processes after shutdown

This is the first end-to-end re-certification of VAL-EXP-001 after the rerun-21 fix combo (`520e75f`, `2acefc3`, `47b60f5`) PLUS the NVENC real-timestamp fix (`be2b653`). VAL-EXP-001's pass is preserved.

## VAL-EXP-010 — verdict and classification

**Fail.** First must-pass red. Runner stopped here.

Cadence analysis (`smoke-evidence-tree/VAL-EXP-010/cadence-analysis.json`):

| Field | Value |
|---|---|
| `frameCount` | **1560** |
| `meanIntervalS` | **0.018283650416933934** (≈ 18.284 ms ⇒ ≈ 54.7 fps) |
| `p95IntervalS` | **0.018766999999999978** (≈ 18.767 ms ⇒ ≈ 53.3 fps) |
| `p99IntervalS` | **0.01938899999999988** (≈ 19.389 ms ⇒ ≈ 51.6 fps) |
| `duplicatedPtsCount` | **0** |
| `nominalIntervalS` | 0.016666666666666666 (60 fps) |

Per-threshold:

| Threshold | Observed | Required | Pass |
|---|---|---|---|
| duplicated PTS ≤ 1 | 0 | ≤ 1 | ✓ |
| cadence mean ±0.5% of nominal | 18.284 ms | 16.583..16.750 ms | ✗ |
| cadence p95 ±2.0% of nominal | 18.767 ms | 16.333..17.000 ms | ✗ |
| cadence p99 ±5.0% of nominal | 19.389 ms | 15.833..17.500 ms | ✗ |
| frame count within ±1 of round(duration × fps) | 1560 | 1711 ± 1 | ✗ |

`pts-summary.json`: `ptsSampleCount = 1560`, `firstPts = 0`, `lastPts = 28.504211` (export duration 28.521 s from `export-terminal-event.json`, 1560 / 28.504 ≈ 54.7 fps).

**Classification:** **variable-rate capture mismatch / validation nominal-fps mismatch** on the export-side gate.

- `duplicatedPtsCount = 0` rules out the source-timestamp / duplicate-PTS bug class. The NVENC real-timestamp fix (`be2b653`) is **verified** end-to-end here — fragment PTS now reflect real capture timestamps, not synthesized round-numbered duplicates. The rerun-21 cadence-analysis (`frameCount=30, meanIntervalS=1.0, duplicatedPtsCount=0` on a 30-sample warmup window) was pre-fix and not directly comparable; what matters is that the 1560-sample post-fix walk shows zero duplicates over the full 28.5-second export.
- `frame count 1560 vs 1711 (×60 fps)` matches the ~54.7 fps observed cadence (1560 / 28.504 ≈ 54.7). Same observed sub-nominal capture rate as VAL-CAP-004 (54.915 mean). The export PTS faithfully reflect the actual upstream capture cadence — the export pipeline is doing the right thing.
- The ffmpeg / remux path is healthy: file exists, plays, SHA256 verified, `mode = stream-copy`.
- The failure is **VAL-EXP-010 still uses strict ±0.5% / ±2.0% / ±5.0% of 60 fps nominal cadence on the export-side**, while the capture is genuinely variable-rate sub-nominal (KWin/PipeWire 60 Hz compositor pattern documented in reruns 20–24). VAL-CAP-004 was relaxed for this in `6f9569c`; VAL-EXP-010 was not.

This is the same physical pattern that motivated ISS-007 (metric redesign for variable-rate capture cadence), now surfaced on the export-side.

## export.completed vs export.failed

- VAL-EXP-001: `export.completed` (8 131 951 bytes, sha verified)
- VAL-EXP-010: `export.completed` (8 121 653 bytes, sha verified) — terminal event is success; the gate failure is on PTS/cadence analysis after a successful export, not on export termination

## Output file existence and size

- VAL-EXP-001: `/tmp/cove-val-exp001-1779404274003-4ff6d61ba397c834/export.mp4` — 8 131 951 bytes at runtime (purged post-row; verified at runtime via `baseline_pgrep.json` + `sha256-check.json`)
- VAL-EXP-010: `/tmp/cove-val-exp-pts-1779404354216-6d00351d68ec44da/export.mp4` — 8 121 653 bytes at runtime (purged post-row; SHA256 captured before purge)

## SHA256 verification

- VAL-EXP-001: helper `c28f222c…f132b8` = runner `c28f222c…f132b8` ⇒ **match**
- VAL-EXP-010: helper `07ee7343…f7b56e` = runner `07ee7343…f7b56e` ⇒ **match**

## ffprobe verification

`smoke-evidence-tree/VAL-EXP-010/ffprobe-frames.json` contains the per-frame walk. First frame: keyframe (I), pts=0, duration=1678 ticks (1678 / 90000 ≈ 0.01864 s ⇒ 53.6 fps inter-frame). Subsequent frames step by ~1678–1700 ticks. No fake duplicated round-second PTS values appear (rerun-21 pattern was eliminated by `be2b653`).

## cadence-analysis (VAL-EXP-010 reached)

frame=1560, meanIntervalS≈0.018284, p95≈0.018767, p99≈0.019389, nominalIntervalS≈0.016667, duplicatedPtsCount=0.

## duplicatedPtsCount (VAL-EXP-010 reached)

`0`. The NVENC real-PTS fix is **verified** end-to-end at the export-fragment / ffmpeg-remux level.

## Frame count (VAL-EXP-010 reached)

`1560` (required `1711 ± 1` ⇒ off by 151). Off-by-~150 is exactly explained by the ~55 fps actual capture rate over the 28.5 s window (28.504 × 60 ≈ 1710, 28.504 × 54.7 ≈ 1559).

## ISS-006 — recommendation

**Can close.** ISS-006 tracked the malformed-trun / data_offset / ffmpeg-failure cluster on VAL-EXP-001. VAL-EXP-001 is now green end-to-end in this rerun (8 131 951 bytes, SHA256-verified, `mode = stream-copy`, terminal `export.completed`, no process leak). The remaining VAL-EXP-010 failure is **not** about export structural correctness (file plays, ffprobe walks cleanly, SHA256 matches, zero duplicated PTS); it is about PTS-cadence policy on a variable-rate capture. ISS-006 close is therefore independent of VAL-EXP-010.

Do not auto-close ISS-006 in this rerun (scope is evidence-only); operator/Tab-1 should close it explicitly.

## ISS-007 — recommendation

**Remains follow-up only — scope expands to export-side.** ISS-007 tracks the variable-rate cadence metric redesign for VAL-CAP-004. Rerun 25 confirms the same variable-rate physical pattern surfaces on VAL-EXP-010 (cadence mean / p95 / p99 / frame-count gates use strict ±0.5% / ±2.0% / ±5.0% of 60 fps nominal, but the capture genuinely delivers ~55 fps under load). The export-side gate needs the same variable-rate policy treatment that VAL-CAP-004 just received (or a unified ISS-007 redesign that covers both capture-side and export-side cadence).

Do not patch VAL-EXP-010 thresholds in this rerun. Tab 1 planning is required (operator decision: which fix path, see "Next recommended phase").

## VAL-EXP-010 — recommendation

VAL-EXP-010 is a real, reproducible failure on a healthy export under a genuinely variable-rate capture. It is **not** the duplicate-PTS / source-timestamp class. The recommended resolution paths (Tab 1 planning required, do not implement in this rerun):

1. **Validation-policy fix (recommended):** widen VAL-EXP-010 cadence gates to a variable-rate range matching the policy already applied to VAL-CAP-004 (key `1080p60-nvenc`, `variableRateCadenceMaxSpreadFps = 20`, mean in `[51..61.2]` fps). The export PTS faithfully reflect capture; if capture-side is policy-accepted at ~55 fps mean, export-side should be too. Frame-count gate similarly needs to accept `round(duration × observedCaptureFps)` rather than `round(duration × nominalFps)`.
2. **Subsume into ISS-007 metric redesign:** if a unified variable-rate cadence metric (with explicit observed-fps reporting) is being designed for ISS-007, VAL-EXP-010 should adopt the same metric instead of getting an independent threshold patch.
3. **Source-side cadence stabilization:** out of scope for now — would require KWin/PipeWire compositor or upstream driver change to deliver 60 fps reliably, and would not unblock T-021 on a reasonable timeline.

## T-021 — recommendation

**Cannot green.** First must-pass red is VAL-EXP-010. T-021's acceptance bar requires every must-pass scripted-local row to pass; one red breaks the run.

Do not manually green T-021.

## T-010c — recommendation

**Stays blocked** behind T-021. T-010c is the smoke/RC execution ticket; it requires T-021 green. T-021 is red on VAL-EXP-010. T-010c cannot unblock from this rerun.

## Confirmation of no source / validation / policy changes

- `git status --short --untracked-files=all` ⇒ only intended writes under `.story/handovers/**` and `/home/sin/Desktop/Claude-Handoff/handoff.md`
- `git diff --check` ⇒ clean
- No edits to `helper/**`, `validation/**`, `electron/**`, `src/**`, `packaging/**`, `.github/**`, `package.json`, `package-lock.json`, `Cargo.toml`, `Cargo.lock`, `.story/tickets/T-021.json`, `.story/tickets/T-010c.json`, `.story/issues/**`

## Next recommended phase

Tab 1 planning for **VAL-EXP-010 variable-rate cadence policy** (option 1 above, or unified ISS-007 metric redesign covering both VAL-CAP-004 and VAL-EXP-010). Path of least change is to extend the variable-rate policy keyed on `1080p60-nvenc` from capture-side cadence to export-side cadence in `validation/**`, using observed capture mean as the reference instead of `nominalFps = 60`. Frame-count gate similarly needs the observed-fps reference.

Do **not** proceed to T-010c (smoke/RC) until VAL-EXP-010 is resolved.

## Commands run

```text
git rev-parse HEAD
git status --short --untracked-files=all
git log --oneline -12
git diff --check

cargo build -p cove-replay-engine --release
cargo test -p cove-replay-engine --lib
cargo test -p cove-replay-engine --test encoder_session
cargo test -p cove-replay-engine --test segment_buffer
npm run typecheck
npm run validate:build
npm run build

mkdir -p .story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-25
kscreen-doctor -o > …/kscreen-doctor-pre.txt          (DP-4 at 3840x2160@239.99)
kscreen-doctor -j > …/kscreen-doctor-pre.json
nvidia-smi > …/nvidia-smi-pre.txt
pgrep -af cove-replay-engine > …/pgrep-cove-pre.txt
pgrep -af ffmpeg > …/pgrep-ffmpeg-pre.txt
uptime > …/uptime-pre.txt

kscreen-doctor output.DP-4.mode.1920x1080@60          (mode change)
kscreen-doctor -o > …/kscreen-doctor-applied.txt      (post-modeset baseline)
kscreen-doctor -j > …/kscreen-doctor-applied.json

RUST_LOG=info,cove_replay_engine=debug \
  node dist-validation/runner.js smoke \
    > …/runner-stdout.txt 2> …/runner-stderr.txt

nvidia-smi > …/nvidia-smi-post.txt
pgrep -af cove-replay-engine > …/pgrep-cove-post.txt
pgrep -af ffmpeg > …/pgrep-ffmpeg-post.txt
kscreen-doctor -o > …/kscreen-doctor-post.txt
kscreen-doctor -j > …/kscreen-doctor-post.json
uptime > …/uptime-post.txt

cp -r validation-artifacts/smoke/2026-05-21T22-53-38-426Z/* \
  .story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-25/smoke-evidence-tree/
```

## Patch verdict (variable-rate spread policy)

The patch under test in this rerun is `6f9569c Relax variable-rate capture spread threshold` (`variableRateCadenceMaxSpreadFps = 20`). It does exactly what it was scoped to do: VAL-CAP-004 transitions from red (15 fps spread > 6 in rerun-24, 12 fps spread > 6 in rerun-22) to green (2.0 fps spread ≤ 20 in rerun-25), with drops still 0 and mean 54.915 fps inside the `[51..61.2]` envelope. The remaining VAL-EXP-010 failure is a separate variable-rate cadence policy gap on the export-side (same physical pattern, different gate), not a regression of this patch.

patch is correct
