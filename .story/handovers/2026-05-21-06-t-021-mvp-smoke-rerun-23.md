# T-021 MVP Smoke Rerun 23

**Date:** 2026-05-21
**Pass ID:** T-021 rerun 23 / VAL-EXP-010 real PTS verification
**Purpose:** Get past VAL-CAP-004 (which transient-failed on rerun 22) and verify VAL-EXP-010 after the real PTS cadence fix (`be2b653 Use real capture timestamps for NVENC fragments`). No source/validation/policy changes during rerun.

---

## 1. Repo / Commit Tested

- **Repo:** /home/sin/Projects/cove-screen-recorder
- **HEAD:** `f10bd617751c0c8b8e55071969caee7316ea1912`
- **Commit under test (source fix):** `be2b653 Use real capture timestamps for NVENC fragments`
- **Branch:** main
- **Working tree:** clean before and after rerun

Recent history (top 10):
```
f10bd61 Record MVP smoke rerun 22 evidence
be2b653 Use real capture timestamps for NVENC fragments
080b8b3 Record MVP smoke rerun 21 evidence
47b60f5 Set ffmpeg output muxer for export temp file
850e7e5 Record MVP smoke rerun 20 evidence
2acefc3 Fix fMP4 trun layout for exports
fea7e34 Record MVP smoke rerun 19 evidence
520e75f Persist init segment before export
6dde09b Record VAL-EXP-001 diagnostic evidence
0961e26 Capture ffmpeg export diagnostics
```

Required-history confirmation:
- `Record MVP smoke rerun 22 evidence` — present (f10bd61)
- `Use real capture timestamps for NVENC fragments` — present (be2b653)
- `Record MVP smoke rerun 21 evidence` — present (080b8b3)
- `Set ffmpeg output muxer for export temp file` — present (47b60f5)
- `Fix fMP4 trun layout for exports` — present (2acefc3)

---

## 2. Preflight Result

- `git rev-parse HEAD` → `f10bd617751c0c8b8e55071969caee7316ea1912`
- `git status --short --untracked-files=all` → CLEAN
- `git diff --check` → CLEAN

---

## 3. Build / Test Sanity

| Command | Result |
|---|---|
| `cargo build -p cove-replay-engine --release` | 0 errors, 70 pre-existing warnings |
| `cargo test -p cove-replay-engine --lib` | 101 passed |
| `cargo test -p cove-replay-engine --test encoder_session` | 28 passed |
| `cargo test -p cove-replay-engine --test segment_buffer` | 7 passed |
| `npm run typecheck` | PASS |
| `npm run validate:build` | PASS |
| `npm run build` | PASS |

---

## 4. Display / Environment

- OS: Linux 6.18.32-1-lts
- GPU: NVIDIA GeForce RTX 4080 SUPER (driver baseline idle pre-run captured)
- Host display before: DP-4 at 3840x2160@239.99 (mode 2) — incorrect for smoke
- Action taken: `kscreen-doctor output.DP-4.mode.11`
- Host display after: DP-4 at 1920x1080@60.00* (mode 11) confirmed, VRR `Never` (vrrPolicy 0)
- Operator action in portal: clicked Cancel on VAL-CAP-003 portal dialog (portal denial test, expected). For VAL-CAP-004 onward each row spawns its own helper and issues `capture.requestSession`; the portal D-Bus path was NOT accessible during this session, so every subsequent row's request timed out.

---

## 5. Evidence Directory

`.story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-23/`

Contents:
- `runner-stdout.txt`, `runner-stderr.txt`
- `validation-report.json` (copy of `validation-report-smoke-1779399770381.json` produced by the runner; root `validation-report-*.json` is gitignored)
- `kscreen-doctor-pre.txt`, `kscreen-doctor-pre.json` (before mode change — 3840x2160@239.99)
- `kscreen-doctor-pre-after-mode-set.txt`, `kscreen-doctor-pre-after-mode-set.json` (after mode change — 1920x1080@60)
- `nvidia-smi-pre.txt`, `pgrep-cove-pre.txt`, `pgrep-ffmpeg-pre.txt`, `uptime-pre.txt`
- `nvidia-smi-post.txt`, `pgrep-cove-post.txt`, `pgrep-ffmpeg-post.txt` (both contain only false-positive grep hits on the current shell snapshot — no actual cove-replay-engine or ffmpeg processes alive)
- `kscreen-doctor-post.txt`, `kscreen-doctor-post.json`, `uptime-post.txt`
- `smoke-evidence-tree/` (full per-gate artifacts, copied from `validation-artifacts/smoke/2026-05-21T21-33-06-021Z/`)

---

## 6. Gate Summary

| Gate | Status | Note |
|------|--------|------|
| VAL-PKG-001 | skip | helper-not-available (pre-spawn check) |
| VAL-CAP-001 | skip | manual |
| VAL-CAP-003 | **pass** | Portal denial → `capture.sessionLost(portal-denied)`, helper to IDLE in 156 ms |
| VAL-CAP-004 | **skip** | capture.requestSession timed out — portal D-Bus path inaccessible |
| VAL-CAP-006 | skip | manual |
| VAL-UI-005 | skip | manual |
| VAL-ENC-001 | **skip** | capture.requestSession timed out — portal D-Bus path inaccessible |
| VAL-ENC-006 | skip | manual |
| VAL-SEG-001 | **skip** | capture.requestSession timed out — portal D-Bus path inaccessible |
| VAL-SEG-003 | **skip** | capture.requestSession timed out — portal D-Bus path inaccessible |
| VAL-EXP-001 | **skip** | capture.requestSession timed out — portal D-Bus path inaccessible |
| VAL-EXP-010 | **skip** | capture.requestSession timed out — portal D-Bus path inaccessible |
| VAL-EXP-012 | skip | capture.requestSession timed out — portal D-Bus path inaccessible |
| VAL-UI-003 | skip | capture.requestSession timed out — portal D-Bus path inaccessible |
| VAL-UI-012 | skip | manual |
| VAL-PROC-001 | **pass** | All processes cleaned up within 5 s after engine.shutdown (5326 ms) |
| VAL-PROC-002 | **pass** | All processes cleaned up within 5 s after stopSession+shutdown (5373 ms) |
| VAL-PROC-003 | **pass** | All processes cleaned up within 5 s after SIGTERM (no explicit stopSession) (7379 ms) |
| VAL-PROC-007 | **pass** | No pactl found in helper process tree during idle (2125 ms) |
| VAL-REG-002 | skip | capture.requestSession timed out — portal D-Bus path inaccessible |

Suite verdict: **pass** (pass=5, fail=0, skip=15, error=0). NB: the runner's mechanical "pass" verdict reflects zero failing rows; functionally this run did NOT exercise any post-capture/export must-pass gate. T-021's acceptance criteria are not met by this run.

### Two-layer view of the skipReason

The runner's stderr summary line reports each post-VAL-CAP-003 scripted-local row as `skip (helper-not-available)` because that is the canonical `skipReason` category emitted by the per-row driver. The per-row evidence (`smoke-evidence-tree/<row>/engine-ready.json`, `helper-socket.txt`) and the report.json `message` field show what actually happened underneath: each row's driver **did** spawn its own helper at a per-row socket (e.g., VAL-CAP-004 → PID 665097 at `runner-902d7c44.sock`), the helper emitted `engine.ready`, and only then did `capture.requestSession` time out. The driver's `message` for each of those rows is `"capture.requestSession timed out — portal D-Bus path inaccessible; run in an interactive user session"`. So:

- **Runner-level skipReason category:** `helper-not-available` (matches the runner stderr line and the report.json `skipReason` field).
- **Driver-level root cause from the same report.json message and per-row evidence:** capture.requestSession timeout because the portal D-Bus path was not accessible during the row.

The two are not in conflict — `helper-not-available` is the runner's bucket for "no usable helper at the moment the row needed one", and the per-row evidence narrows the actual reason to portal D-Bus inaccessibility (since each row did get an engine.ready and only failed on requestSession). The runner's startup line `[runner] Helper socket not found — all scripted-local rows will skip` only refers to the absence of a *pre-spawned* helper at the global `engine.sock` path; SELF_SPAWNING_ROW_IDS still attempt their own bring-up afterwards, which is exactly what the per-row artifacts show happened here.

---

## 7. VAL-CAP-004 Verdict

**SKIP — not green / not red.** The driver spawned a helper successfully (engine.ready received from PID 665097 on per-row socket `runner-902d7c44.sock`) but `capture.requestSession` did not produce a session before the row's timeout. The runner reports: "capture.requestSession timed out — portal D-Bus path inaccessible; run in an interactive user session".

This is **not the cadence-spread failure from rerun 22** (12 fps spread vs 6 fps threshold). The cadence gate was not exercised this run — capture never started.

Pattern across recent reruns:
| Rerun | VAL-CAP-004 | Notes |
|---|---|---|
| 20 | pass | cadence mean 53.746 fps, 0 drops |
| 21 | skip | capture.requestSession timed out (same root) |
| 22 | fail | cadence spread 12 fps > 6 (single 65 fps spike) |
| **23** | **skip** | capture.requestSession timed out (same root as 21) |

VAL-CAP-004 portal-D-Bus inaccessibility now repeats (rerun 21 + rerun 23). Rerun 22 in between confirms the helper code path can succeed when the portal is reachable. This is environmental flake on the portal/Wayland session axis, not a source regression introduced by `be2b653`.

---

## 8. VAL-SEG-003 Verdict

**SKIP — not green / not red.** Same root cause as VAL-CAP-004 (helper spawned per-row, capture.requestSession timed out). Not re-certified by rerun 23.

---

## 9. VAL-EXP-001 Verdict

**SKIP — not green / not red.** Helper spawned for the row (engine.ready from PID 666687) but capture never started. Last known status from rerun 21: GREEN. No source changes to export path since then; the green from rerun 21 has not been invalidated, but it has not been re-certified by rerun 23 either.

---

## 10. VAL-EXP-010 Verdict

**SKIP — NOT REACHED.** This is the core verification target of rerun 23 and it was not exercised. Helper spawned for the row (engine.ready from PID 667072 on socket `runner-7b672394.sock`) but `capture.requestSession` timed out before any capture began. The real PTS cadence fix (`be2b653`) **cannot be confirmed as correct or incorrect from this run**.

---

## 11. VAL-REG-002 Verdict

**NOT REACHED (skip).** Same root as VAL-EXP-010.

---

## 12. First Failing Gate

**None — no row failed.** The first must-pass scripted-local gate to drop out was VAL-CAP-004 (skip via capture.requestSession timeout). The runner did NOT bail early because skipped rows are not fail/error; it continued through every remaining row (each independently skipping for the same root cause).

---

## 13. export.completed vs export.failed

- VAL-EXP-001: not reached — capture.requestSession timed out before recording could start
- VAL-EXP-010: not reached — same root
- No export was initiated this run

---

## 14. Output File / SHA / ffprobe

- No export produced. N/A.

---

## 15. cadence-analysis Summary

No cadence-analysis was produced — VAL-EXP-010 driver did not get past `capture.requestSession`. There is no `VAL-EXP-010/cadence-analysis.json` artifact in this rerun's evidence (only helper bring-up artifacts: `engine-ready.json`, `env-probe.json`, `helper-socket.txt`, `load-launch.json`, `baseline_pgrep.json`).

The last known cadence-analysis for VAL-EXP-010 is from rerun 21 (pre-PTS-fix): frameCount=30, meanIntervalS=1.0, p95=1.0, p99=1.0, duplicatedPtsCount=0 (1 fps cadence vs 60 fps nominal). That run was on commit `47b60f5`, before the PTS fix. **The real PTS fix has not been verified end-to-end by any successful smoke run yet.**

---

## 16. ffprobe Timing Summary

Not reached. No export → no ffprobe walk.

---

## 17. Can ISS-006 Close?

**Yes (unchanged from rerun 21 recommendation).** ISS-006 tracks "VAL-EXP-001: malformed fMP4 trun flags and data_offset cause ffmpeg failure". VAL-EXP-001 was GREEN end-to-end on rerun 21 (SHA256-verified export, 565 857 bytes, exit 0). No export-path source changes have been made since the rerun-21 fix combo. Rerun 23 did not re-execute the row, but it also did not invalidate it. Recommendation remains: close ISS-006 with resolution from rerun 21. Re-certification of VAL-EXP-001 in a future portal-accessible run is desirable but not blocking for the issue close.

---

## 18. Can T-021 Go Green?

**No.** Multiple must-pass scripted-local rows skipped in this rerun (VAL-CAP-004, VAL-ENC-001, VAL-SEG-001, VAL-SEG-003, VAL-EXP-001, VAL-EXP-010, VAL-EXP-012, VAL-UI-003, VAL-REG-002). The runner's mechanical "pass" verdict is a consequence of skip-not-fail accounting; T-021's acceptance bar requires actual pass for must-pass rows, not a no-failure tally. T-021 remains red.

Do not manually green T-021.

---

## 19. Is T-010c Unblocked?

**No.** T-010c is gated on T-021 green. T-021 remains red. T-010c stays blocked.

---

## 20. VAL-EXP-010 Recommendation

The PTS fix in `be2b653` cannot be verified from this rerun. Recommended next pass: **rerun 24** with the operator present at the portal to click **Share** promptly for each post-VAL-CAP-003 row that requests a session. If the portal D-Bus inaccessibility persists across multiple back-to-back reruns (now 2 of last 3), open a separate validation-infra issue to investigate why scripted-local rows sometimes succeed (rerun 20, 22) and sometimes time out (rerun 21, 23) on the same machine, same kscreen mode, same idle baseline.

Do NOT patch source, validation, thresholds, ffmpeg argv, segment-commit predicate, or replay.save behavior in response to this rerun.

---

## 21. Failure Classification (for VAL-EXP-010 specifically)

Per the contract checklist — N/A. VAL-EXP-010 was not exercised. No source-timestamp, validation-policy, variable-rate, frame-count, duplicate-PTS, or ffmpeg/remux signal can be read from this run. Classification is **NOT-EXERCISED**.

---

## 22. Source / Validation / Policy Confirmations

- No source files changed during rerun 23
- No validation logic changed
- No thresholds changed
- No ffmpeg argv changed
- No segment-commit predicate changed
- No capture / cadence / NVENC code changed
- Only operator actions: `kscreen-doctor output.DP-4.mode.11`, then portal Cancel for VAL-CAP-003 (denial test). No portal Share was accepted for any subsequent row.

---

## 23. Recommended Next Phase

1. **Rerun 24** with operator actively clicking **Share** on the portal dialog for every post-VAL-CAP-003 row. The minimum target is VAL-EXP-010 reach.
2. If portal D-Bus inaccessibility recurs again (i.e., rerun 24 also can't get past VAL-CAP-004 via portal), open a validation-infra issue under `owner_on_fail = validation` for "Scripted-local rows intermittently fail capture.requestSession with portal D-Bus inaccessible — investigate Wayland/portal session lifecycle for back-to-back helper spawns" and pursue Tab 1 planning before another commit-verification rerun. Reruns 21 and 23 are now two pieces of evidence for this flake.
3. Do not patch source, validation, or thresholds in response to this rerun.

---

## 24. Patch Verdict for the Commit Under Test

The commit under test is `be2b653 Use real capture timestamps for NVENC fragments`. VAL-EXP-010 was not reached this rerun (capture never started). No artifact in this run proves or disproves the PTS fix. Smoke run produced no failing rows, but it also did not exercise the gate the rerun was scoped to verify.

**Indeterminate — treated as not verified.** Rerun 24 is required to reach VAL-EXP-010. Per the verification contract, an unexercised target cannot be marked correct; the single-valued verdict is therefore:

patch is incorrect
