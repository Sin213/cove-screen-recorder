# T-021 MVP Smoke Rerun 27 — Full suite green / VAL-REG-002 first reach

- Date (local): 2026-05-21 19:21–19:31
- Repo: /home/sin/Projects/cove-screen-recorder
- Commit tested: `65546e4ffe086bf7327ff2a1135f33d0ebe527ea`
- Pass id: T-021 rerun 27 / full smoke green candidate
- Evidence dir: `.story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-27/`
- Runner artifact: `validation-artifacts/smoke/2026-05-22T02-21-35-097Z/`
- Runner exit code: 0

## Scope confirmation

Evidence/verification pass only.

- No source files touched (`helper/**`, `electron/**`, `src/**` clean)
- No validation logic touched (`validation/**` clean)
- No thresholds changed (`thresholds.json` reflects committed values from `65546e4`)
- No `package.json` / `package-lock.json` / `Cargo.toml` / `Cargo.lock` touched
- No ticket / issue files touched (`.story/tickets/T-021.json`, `.story/tickets/T-010c.json`, `.story/issues/**`)
- Only allowed writes: `.story/handovers/2026-05-21-11-t-021-mvp-smoke-rerun-27.md`, `.story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-27/**`, `/home/sin/Desktop/Claude-Handoff/handoff.md`

## Required-history confirmation

- `Allow fast export UI policy for stream copy` — present (65546e4, **patch under test**)
- `Record MVP smoke rerun 26 evidence` — present (f565ef0)
- `Mirror variable-rate cadence policy for export validation` — present (9356569)
- `Record MVP smoke rerun 25 evidence` — present (08613f6)
- `Relax variable-rate capture spread threshold` — present (6f9569c)
- `Use real capture timestamps for NVENC fragments` — present (be2b653)

## Build / test sanity (all green)

| Command | Result |
|---|---|
| `cargo build -p cove-replay-engine --release` | 0 errors, 70 pre-existing warnings |
| `cargo test -p cove-replay-engine --lib` | 101 passed |
| `cargo test -p cove-replay-engine --test encoder_session` | 28 passed |
| `cargo test -p cove-replay-engine --test segment_buffer` | 7 passed |
| `npm run typecheck` | pass |
| `npm run validate:build` | pass |
| `npm run build` | pass (vite renderer + tsc electron) |

## Display / environment

- DP-4 before mode set: 3840x2160@239.99 (mode 2). Pre-evidence: `kscreen-doctor-pre-before-modeset.{txt,json}`.
- Action: `kscreen-doctor output.DP-4.mode.1920x1080@60`.
- DP-4 at run time: 1920x1080@60.00 (mode 11), VRR `Never`, HDR enabled, Wide Color Gamut enabled. Confirmation snapshot: `kscreen-doctor-pre.{txt,json}`.
- GPU: NVIDIA RTX 4080 SUPER, driver 595.71.05, CUDA 13.2. GPU-Util pre = **5%** (P8 idle), memory used 4154 MiB / 16376 MiB.
- Background workload present on the GPU (informational, no impact on gating): `mpv` (C+G, 851 MiB — actively decoding), `vesktop` (G, 1098 MiB), `kwin_wayland` (G, 439 MiB), plus the usual KDE/Firefox/Dolphin set. Pre-run `pgrep -af ffmpeg` also caught one transient unrelated user-space ffmpeg process (not under `cove-replay-engine` parentage); it finished before the run completed and is **not** in the post-run pgrep (see VAL-PROC-001/002/003 evidence — runner-owned process gates were zero).
- `pgrep-{cove,ffmpeg}-{pre,post}.txt` are sanitized summaries: counts of matches and a confirmation that any non-grep matches were unrelated user-space processes (no real `cove-replay-engine` process pre or post; no runner-owned `ffmpeg` post). Sanitized to redact unrelated shell command lines and session metadata that `pgrep -af` captured incidentally.
- `uptime` pre = `2 days, 10:09, load 1.65 / 1.28 / 1.13`; post = `2 days, 10:19, load 1.02 / 1.15 / 1.14`.

## Suite verdict

| Metric | Value |
|---|---|
| Verdict | **PASS** |
| pass / fail / skip / error | **14 / 0 / 6 / 0** |
| Stopped early at | n/a (runner walked the full smoke set) |
| First failing gate | **none** |
| Runner exit code | `0` |

## Gate verdicts (full matrix)

| Row | Status | Notes |
|---|---|---|
| VAL-PKG-001 | skip | `helper-not-available` (runner spawns helper per row; runner-owned socket not present for the env-probe row — same artifact pattern as every prior rerun) |
| VAL-CAP-001 | skip | `manual` |
| VAL-CAP-003 | **pass** | Portal denial emitted `capture.sessionLost(portal-denied)`; helper to IDLE in 156 ms |
| VAL-CAP-004 | **pass** | 1080p60 NVENC monitor capture: drop=0, mean=54.508 fps, spread=3.000 fps, 60 diagnostics samples |
| VAL-CAP-006 | skip | `manual` |
| VAL-UI-005 | skip | `manual` |
| VAL-ENC-001 | **pass** | `encoder.probeResult` received, NVENC available=true, `encoder.selected = nvenc` |
| VAL-ENC-006 | skip | `manual` |
| VAL-SEG-001 | **pass** | Segment buffer 64 samples, `buffer_bytes_pct_of_cap` max 0.19%, full 65 s window observed |
| VAL-SEG-003 | **pass** | `replay.save` completed in 0 ms (gate 2250 ms); `replay.snapshotPinned` received; `active_snapshots = 0` after release |
| VAL-EXP-001 | **pass** | `export.completed`, 7 846 176 bytes, SHA256 verified, `mode = stream-copy`, no process leak |
| VAL-EXP-010 | **pass** | **0 duplicated PTS**, cadence within variable-rate policy, 1560 frames in 28.800 s (mean 54.163 fps, inside `[1468..1763]`) |
| VAL-EXP-012 | **pass** | Concurrent export: during-export drops=0, `export.completed`, `encoder.selected = nvenc`, 7 718 675 bytes |
| **VAL-UI-003** | **pass** | **fast-export branch active** (windowS=0, terminalMethod=`export.completed`), static assertions pass |
| VAL-UI-012 | skip | `manual` |
| VAL-PROC-001 | **pass** | All processes cleaned up within 5 s after `engine.shutdown` |
| VAL-PROC-002 | **pass** | All processes cleaned up within 5 s after `stopSession+shutdown` |
| VAL-PROC-003 | **pass** | All processes cleaned up within 5 s after SIGTERM (no explicit `stopSession`) |
| VAL-PROC-007 | **pass** | No pactl in helper process tree during idle |
| **VAL-REG-002** | **pass** | **First time reached.** No fake-60fps duplication: 0 dup PTS in 28.7 s |

## VAL-UI-003 — patch verification (primary goal of rerun)

Patch under test: `65546e4 Allow fast export UI policy for stream copy`. Goal: VAL-UI-003 must green via a `fast-export` branch when the stream-copy export terminates within `windowS < 3`, while preserving the static UI assertions (v2 clock active for RECORDING/SAVING/EXPORTING, rAF-driven `useV2ElapsedMs`).

### Per-threshold (VAL-UI-003)

| Threshold | Observed | Required | Pass | Gating |
|---|---|---|---|---|
| export completed successfully (not failed or cancelled) | `export.completed` | `export.completed` | ✓ | yes |
| EXPORTING window ≥ 3 seconds (fast-export informational) | 0 s (126 ms wall-clock) | ≥ 3 | ✗ | **no (informational under fast-export)** |
| HUD diagnostics at ≥ 1 Hz during EXPORTING window (fast-export informational) | passed (0 samples over 0 s) | ≥ 1 capture.diagnostics / s | ✓ | no (vacuous on 0-s window) |
| **fast-export branch active (windowS < 3): EXPORTING state observed; HUD/freeze regression covered by static assertions** | `{"windowS":0,"diagCount":0,"terminalMethod":"export.completed"}` | `windowS < 3 AND terminalMethod === "export.completed"` | ✓ | **yes** |
| App.tsx: v2 clock active for RECORDING/SAVING/EXPORTING with `v2SessionReadyMs` guard | assertion matched | static assertion | ✓ | yes |
| src/v2/clocks.ts: `useV2ElapsedMs` is rAF-driven | `requestAnimationFrame` found | static assertion | ✓ | yes |

### Confirmations requested by handoff

- **`policy = fast-export` for `windowS < 3`**: confirmed. `hud-hz-check.json` shows `windowDurationMs = 126`, `windowS = 0`, `diagCount = 0`; runner message reads `HUD Hz validated (policy=fast-export): 0 diagnostics over 0s export window; static assertions passed`. The fast-export branch in `validation/drivers.ts` activated correctly with the gating threshold "fast-export branch active (windowS < 3)" returning `passed = true`.
- **Static assertions pass**: confirmed. Both `app-clock-assertion.json` and `clocks-raf-assertion.json` are present and the row's `App.tsx: v2 clock active …` and `src/v2/clocks.ts: useV2ElapsedMs is rAF-driven` thresholds returned `passed = true`.
- **`export.completed` gate passes**: confirmed. `export-terminal-event.json` shows `method = export.completed`, terminal-event gate `passed = true`.

### Classification

VAL-UI-003 transitions from rerun-26 **fail** (strict `EXPORTING window ≥ 3 s` gate tripped at 119 ms wall-clock under stream-copy) to rerun-27 **pass** via the policy patch. The strict-window thresholds are still emitted in the evidence stream but are marked `gating: false` under the `fast-export` branch; the new gating threshold ("fast-export branch active …") asserts the policy was active and that the terminal event is `export.completed`. The HUD/freeze regression continues to be enforced by the static assertions on `App.tsx` and `src/v2/clocks.ts`.

## VAL-REG-002 — first-reach evidence (load-bearing)

`smoke-evidence-tree/VAL-REG-002/`:

- `cadence-analysis.json`:
  ```
  frameCount:         1560
  meanIntervalS:      0.018403983964079540   (≈ 18.404 ms ⇒ ≈ 54.34 fps)
  p95IntervalS:       0.019633999999999930
  p99IntervalS:       0.020044999999999646
  duplicatedPtsCount: 0
  nominalIntervalS:   0.016666666666666666
  ```
- `export-terminal-event.json`: `export.completed`, 7 771 030 bytes, `duration_s = 28.708477777777777`, `mode = stream-copy`.
- `sha256-check.json`: helper `1343541862…071d4` = runner `1343541862…071d4` ⇒ **match**, file size 7 771 030 bytes.

### Per-threshold (VAL-REG-002)

| Threshold | Observed | Required | Pass |
|---|---|---|---|
| duplicated PTS ≤ 1 (≤ 1/min) | **0** | ≤ 1 | ✓ |
| cadence stats (informational) | mean=18.404 ms p95=19.634 ms p99=20.045 ms | nominal=16.667 ms | ✓ (informational) |

### `duplicatedPtsCount` invariant and duplicate-PTS allowance

`duplicatedPtsCount = 0` over the full 1560-sample, 28.7 s walk. The duplicate-PTS gate (≤ 1) is preserved end-to-end at the regression-guard level. The duplicate-PTS allowance of "`≤ 1` per export window" remains in effect (one allowance per `≤ 1/min`) and was not consumed. The NVENC real-timestamp fix from `be2b653` continues to hold at both the export gate (VAL-EXP-010) and the regression gate (VAL-REG-002).

This is the **first end-to-end observation** of VAL-REG-002 in any rerun on record — reruns 20–26 all stopped earlier in the suite (VAL-EXP-001 / VAL-EXP-010 / VAL-CAP-004 / VAL-UI-003 depending on the rerun), so VAL-REG-002 was "not reached" in every prior pass. Reaching and greening it in rerun 27 closes the last load-bearing T-021 acceptance row.

## VAL-EXP-001 — verdict

Pass. `export.completed`, 7 846 176 bytes, SHA256 `d2cacbbd…b56cf` (helper = runner match), `mode = stream-copy`, `duration_s = 28.7229 s` (informational duration check non-gating). No process leak (`baseline_pgrep.json` / `post_pgrep.json` clean).

## VAL-EXP-010 — verdict and duplicatedPtsCount

Pass. Variable-rate cadence policy from `9356569` continues to hold under stream-copy export.

`exp-cadence-policy.json`:

```
captureFormat: { width: 1920, height: 1080, fps_num: 0, fps_den: 1 }
nominalFps:                              60
nominalSource:                           "row-config"
isVariableRate:                          true
cadencePolicy:                           "variable-rate"
meanFps:                                 54.16266106747275
frameCount:                              1560
durationS:                               28.800333333333334
variableRateCadenceMinFracOfNominal:     0.85
variableRateCadenceMaxFracOfNominal:     1.02
variableRateCadenceMaxSpreadFps:         20
```

`cadence-analysis.json`:

```
frameCount:         1560
meanIntervalS:      0.018462903784477230   (≈ 18.463 ms ⇒ ≈ 54.16 fps)
p95IntervalS:       0.019766000000000616
p99IntervalS:       0.020156000000000063
duplicatedPtsCount: 0
nominalIntervalS:   0.016666666666666666
```

`export-terminal-event.json`: `export.completed`, 7 891 522 bytes, `mode = stream-copy`, SHA256 `05601629…85b1`.

**duplicatedPtsCount = 0**. The duplicate-PTS gate is preserved (≤ 1 allowance not consumed). All gating thresholds pass; the strict ±0.5% / ±2.0% / ±5.0% rows remain `gating: false` per `9356569`.

## VAL-EXP-012 — verdict

Pass. Concurrent-export-vs-recording row:

| Threshold | Observed | Required | Pass |
|---|---|---|---|
| baseline diagnostics samples | 75 | ≥ 5 | ✓ |
| during-export diagnostics samples | 1 | ≥ 1 | ✓ |
| `export.completed` (not failed/cancelled) | export.completed | export.completed | ✓ |
| export file exists on disk | yes (7 718 675 bytes) | exists | ✓ |
| `capture.sessionLost` not received during export | ok | no session loss | ✓ |
| `encoder.selected` backend | nvenc | nvenc | ✓ |
| during-export capture drop rate ≤ 0 | **0.000000** | ≤ 0 | ✓ |

## VAL-CAP-004 — verdict and portal behavior

Pass. Portal picker behavior on this rerun was clean: operator selected DP-4 1920x1080@60 promptly inside the runner's 60 s `requestSession` budget. No focus changes during `requestSession`. No `busctl --user list` snapshot required (no operator-evidence file written — `operator-evidence/` directory created empty as part of preflight, no anomaly observed).

| Threshold | Observed | Required | Pass | Gating |
|---|---|---|---|---|
| capture cell matches declared (1920x1080) | 1920x1080 | 1920x1080 | ✓ | yes |
| drop rate ≤ 0 (1080p60-nvenc) | 0.000000 (warmup=1, effectiveSamples=59, excludedDropped=17, dropEffectiveProduced=3216) | ≤ 0 | ✓ | yes |
| cadence mean in variable-rate range [51.00..61.20] fps | **54.508** | 51.00..61.20 | ✓ | yes |
| cadence spread ≤ 20.0 fps (variable-rate compositor variance) | **3.000** | ≤ 20.0 | ✓ | yes |
| cadence mean within ±0.5% of 60.00 fps (informational) | 54.508 | 59.70..60.30 | ✗ | no |
| diagnostics samples received | 60 | ≥ 1 | ✓ | yes |
| `encoder.selected` backend | nvenc | nvenc | ✓ | yes |

Pattern matches reruns 25/26: KWin/PipeWire compositor delivers sustained sub-nominal cadence (~54.5 fps mean) under load, drops are 0, spread well inside the `≤ 20 fps` envelope from `6f9569c`. The variable-rate capture-side policy holds without modification.

## VAL-SEG-003 — verdict

Pass. `replay.save` completed in 0 ms (gate 2250 ms). `replay.snapshotPinned` received. 3 capture diagnostics received during/after save (capture not paused). `active_snapshots` returned to 0 after release.

## VAL-PROC-001 / VAL-PROC-002 / VAL-PROC-003 / VAL-PROC-007 — verdicts (process leak checks)

All four process-cleanup rows green:

- **VAL-PROC-001**: 0 cove-replay-engine / 0 ffmpeg / 0 pactl after `engine.shutdown` (5 317 ms wall)
- **VAL-PROC-002**: 0 cove-replay-engine / 0 ffmpeg / 0 pactl after `stopSession + shutdown` (5 372 ms wall)
- **VAL-PROC-003**: 0 cove-replay-engine / 0 ffmpeg / 0 pactl after SIGTERM (no explicit `stopSession`, 7 383 ms wall)
- **VAL-PROC-007**: no `pactl` in helper process tree during idle (2 124 ms wall)

External (sanitized) `pgrep -af cove-replay-engine` and `pgrep -af ffmpeg` post-run confirm only the false-positive grep hit for the bash command itself; no real cove or ffmpeg processes remain. The transient unrelated user-space ffmpeg observed in `pgrep-ffmpeg-pre.txt` is not in `pgrep-ffmpeg-post.txt` (it finished during the 9-minute run and is not under `cove-replay-engine` parentage — VAL-PROC-001/002/003 evidence under `smoke-evidence-tree/VAL-PROC-*/post_pgrep.txt` shows zero cove-owned processes).

## Patch verdict (fast-export UI policy for stream copy)

The patch under test is `65546e4 Allow fast export UI policy for stream copy`. It does exactly what it was scoped to do:

- VAL-UI-003 transitions from rerun-26 **fail** (strict `EXPORTING window ≥ 3 s` tripped at 119 ms wall-clock) to rerun-27 **pass** (fast-export branch active, `windowS = 0`, terminalMethod = `export.completed`, gating threshold returns `true`).
- Strict-window thresholds (`EXPORTING window ≥ 3 seconds`) still emit but are marked `gating: false` under the fast-export branch (evidence retained for diagnostics).
- Static assertions on `App.tsx` v2 clock and `src/v2/clocks.ts` rAF-driven `useV2ElapsedMs` continue to enforce the HUD/freeze regression invariant — the fast-export branch does not weaken Issue #4 coverage.
- VAL-EXP-010 / VAL-EXP-012 / VAL-CAP-004 / VAL-SEG-003 / VAL-REG-002 are unaffected (no regression of `9356569`, `6f9569c`, or `be2b653`).

**patch is correct**

## T-021 — recommendation

**Ready to green.** Full smoke suite green on commit `65546e4`:

- All 14 scripted-local must-pass rows pass.
- All 6 skips are deliberate (5 manual rows that are operator-driven and explicitly out-of-scope for the auto runner; 1 `helper-not-available` skip for `VAL-PKG-001` which the per-row helper-spawning runner pattern produces on every rerun and is not part of T-021's MVP must-pass surface).
- VAL-REG-002 reached and green for the first time on record — the duplicate-PTS regression-guard row is now exercised end-to-end and confirms `duplicatedPtsCount = 0` post-fix.
- VAL-UI-003 green via the policy-correct fast-export branch with HUD/freeze regression preserved by static assertions.
- No process leak (VAL-PROC-001/002/003/007 all green; external pgrep clean).
- No source/runtime/validation regressions relative to reruns 25/26.

Per handoff scope, **do not green T-021 in this rerun.** Operator/Tab-1 should mark T-021 complete in a separate ticket-update commit, citing this handover and `validation-artifacts/smoke/2026-05-22T02-21-35-097Z/report.json`.

## T-010c — recommendation

**Can unblock after T-021 is greened.** T-010c's `blockedBy` list (`["T-010a","T-010b","T-021"]`) is satisfied for the T-021 dependency by this rerun. T-010a / T-010b status not changed by this rerun.

Per handoff scope, **do not unblock T-010c in this rerun.** The operator/Tab-1 should update T-010c's `blockedBy` (or run a ticket-status pass) in the same commit that greens T-021, citing this handover.

## ISS-006 — recommendation

**Can close.** ISS-006 tracked the malformed-trun / data_offset / ffmpeg-failure cluster on VAL-EXP-001. VAL-EXP-001 has been green for three consecutive reruns now (25, 26, 27) under `mode = stream-copy` with SHA256-verified output and clean process cleanup. The closure is independent of any cadence-policy state.

Per handoff scope, **do not close ISS-006 in this rerun.** Operator/Tab-1 should close it explicitly when greening T-021.

## ISS-007 — recommendation

**Remains follow-up only.** ISS-007 tracks the variable-rate cadence metric redesign (observed-fps reporting to operators, unified capture+export cadence metric). Capture-side (`6f9569c`) and export-side (`9356569`) variable-rate policies are now in place and have produced three consecutive green VAL-CAP-004 / VAL-EXP-010 reruns. The remaining ISS-007 scope (unified metric, operator-facing observed-fps surfacing) is not required for T-021 acceptance.

Per handoff scope, **do not change ISS-007 in this rerun.** If the maintainer wants the unified metric, that work continues under ISS-007 after T-021 is greened.

## Confirmation of no source / validation / policy / ticket / issue changes

- `git status --short --untracked-files=all` ⇒ only intended writes under `.story/handovers/**` and `/home/sin/Desktop/Claude-Handoff/handoff.md`
- `git diff --check` ⇒ clean
- No edits to `helper/**`, `validation/**`, `electron/**`, `src/**`, `packaging/**`, `.github/**`, `package.json`, `package-lock.json`, `Cargo.toml`, `Cargo.lock`, `.story/tickets/T-021.json`, `.story/tickets/T-010c.json`, `.story/issues/**`

## Next recommended phase

1. **Tab 1 ticket pass (operator/maintainer)**: green T-021, close ISS-006, update T-010c to remove the `T-021` `blockedBy` entry, all in a single ticket-status commit referencing this handover.
2. **T-010c kickoff**: once T-021 is greened, T-010c (smoke/RC execution against the full M1+M2+M3+M4 matrix) becomes the next executable ticket. Plan the M2/M3/M4 environment setup and the full 1080p/1440p/4K60 must-pass attempt.
3. **ISS-007 follow-up (optional)**: unified observed-fps cadence metric for capture+export, if maintainer wants operator-facing surfacing of the sub-nominal compositor delivery. Not blocking for T-010c.

## Commands run

```text
git rev-parse HEAD
git status --short --untracked-files=all
git log --oneline -12
git diff --check

cargo build -p cove-replay-engine --release
cargo test -p cove-replay-engine --lib
cargo test -p cove-replay-engine --test encoder_session
cargo test -p cove-replay-engine --test segment_buffer
npm run typecheck
npm run validate:build
npm run build

mkdir -p .story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-27/operator-evidence
mkdir -p .story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-27/smoke-evidence-tree

kscreen-doctor -o > …/kscreen-doctor-pre-before-modeset.txt   (DP-4 at 3840x2160@239.99)
kscreen-doctor -j > …/kscreen-doctor-pre-before-modeset.json
kscreen-doctor output.DP-4.mode.1920x1080@60                   (mode change)
kscreen-doctor -o > …/kscreen-doctor-pre.txt                   (post-modeset baseline)
kscreen-doctor -j > …/kscreen-doctor-pre.json
nvidia-smi > …/nvidia-smi-pre.txt
pgrep -af cove-replay-engine > …/pgrep-cove-pre.txt
pgrep -af ffmpeg > …/pgrep-ffmpeg-pre.txt
uptime > …/uptime-pre.txt

RUST_LOG=info,cove_replay_engine=debug \
  node dist-validation/runner.js smoke \
    > …/runner-stdout.txt 2> …/runner-stderr.txt
echo runner exit=$? > …/runner-exit-code.txt

nvidia-smi > …/nvidia-smi-post.txt
pgrep -af cove-replay-engine > …/pgrep-cove-post.txt
pgrep -af ffmpeg > …/pgrep-ffmpeg-post.txt
kscreen-doctor -o > …/kscreen-doctor-post.txt
kscreen-doctor -j > …/kscreen-doctor-post.json
uptime > …/uptime-post.txt

cp -a validation-artifacts/smoke/2026-05-22T02-21-35-097Z/. \
  .story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-27/smoke-evidence-tree/
```
