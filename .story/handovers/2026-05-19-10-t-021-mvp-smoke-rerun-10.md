# T-021 MVP Smoke Rerun 10 — Post-Path-D Fix Cadence/Drop Re-Entry Evidence

**Date:** 2026-05-19
**Commit tested:** `803e402` (Fix PipeWire SHM retry framerate fallback)
**Verdict:** RED-BOTH — capture.sessionReady restored, no-acceptable-buffer-type regression closed, but cadence and drop-rate gates still fail; same family of failure as rerun 8 (pre-existing PipeWire delivery-rate issue).

---

## 1. Repo / Commit Tested

- Repo: `/home/sin/Projects/cove-screen-recorder`
- HEAD: `803e402b654a26465a24a9d50fb45f76dd642b50`
- Branch: `main`
- Recent history (top 8):
  - `803e402` Fix PipeWire SHM retry framerate fallback
  - `5bfacfa` Record MVP smoke rerun 9 evidence
  - `b5db18b` Implement PipeWire framerate hint
  - `bf6a339` Record MVP smoke rerun 8 evidence
  - `814034f` Record MVP smoke rerun 7 evidence
  - `d5f39b0` Record MVP smoke rerun 6 evidence
  - `1260dd2` Fix NVENC encode path FFI layout
  - `9a5ff33` Fix NVENC probe FFI versions

## 2. Preflight Result

- `git status --short --untracked-files=all` — no tracked-file modifications; the snapshot file `preflight-git-status.txt` was captured after the rerun-10 evidence directory and `preflight-head.txt` were created, so it shows exactly two `??` entries for those just-written evidence files and nothing else. No source / helper / validation / electron / packaging / Cargo / package.json / story-config files are dirty.
- `git diff --check` — clean (zero bytes)
- HEAD is `803e402`, the confirmed commit under test
- Build/validation sanity:
  - `cargo build -p cove-replay-engine --release` — OK (pre-existing NVENC warnings only)
  - `cargo test -p cove-replay-engine -- --test-threads=1` — all green
  - `npm run typecheck` — OK
  - `npm run validate:build` — OK
  - `npm run build` — OK (vite + electron tsc)

Preflight evidence: `evidence/2026-05-19-t-021-mvp-smoke-rerun-10/preflight-*` and `build-sanity.txt`.

## 3. Environment Summary

- Session: KDE Wayland (`WAYLAND_DISPLAY=wayland-0`)
- Portal: `xdg-desktop-portal-kde` (PID 2561) running
- Output: DP-4 (DisplayPort, single monitor), NVIDIA RTX 4080
- Display mode before run: switched from 3840x2160@239.99 (mode 2) → **1920x1080@60.00 (mode 11)** using `kscreen-doctor output.DP-4.mode.11`
- Display mode during/after run: 1920x1080@60.00 (mode 11), confirmed active
- VRR / adaptive sync: `vrrPolicy: 0` / `Vrr: Never` — confirmed disabled
- Operator selected DP-4 in the KDE portal and clicked Share promptly
- Pre-run helper/ffmpeg: no real `cove-replay-engine` and no real `ffmpeg` processes (the only pgrep matches were the wrapping shell command itself, which contains the literal string in its arg list)

Pre-/post-run state files:
- `display-mode-before.{txt,json}`, `display-mode-after.txt`
- `nvidia-smi-before.txt`, `nvidia-smi-after.txt`
- `pgrep-cove-before.txt`, `pgrep-cove-after.txt`
- `pgrep-ffmpeg-before.txt`, `pgrep-ffmpeg-after.txt`
- `portal-status-before.txt`
- `host-load-before.txt`, `host-load-after.txt`

## 4. Exact Validation Command(s)

```
RUST_LOG=info,cove_replay_engine=debug \
  node dist-validation/runner.js smoke \
  > .story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-10/runner-stdout.txt \
  2> .story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-10/runner-stderr.txt
```

Runner exit code: 1 (expected — VAL-CAP-004 must-pass red).
Evidence root used by the runner: `validation-artifacts/smoke/2026-05-20T06-18-25-043Z/` (copied into the rerun directory as `smoke-evidence-tree/`).

## 5. Evidence Directory Path

`/home/sin/Projects/cove-screen-recorder/.story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-10/`

Key files:
- `runner-stdout.txt`, `runner-stderr.txt`
- `validation-report.json` (top-level runner report)
- `smoke-evidence-tree/report.json`
- `smoke-evidence-tree/VAL-CAP-004/{thresholds,capture-diagnostics,sessionReady-notification,startStream-response,encoder-selected,encoder-probe-result,engine-ready,requestSession-response,stopSession-response,shutdown-response,load-launch,helper-socket,env-probe}.json|.txt`
- `smoke-evidence-tree/VAL-CAP-003/{requestSession-response,startStream-response,sessionLost-notification,shutdown-response,engine-health}.json`

## 6. Rerun 8 vs Rerun 9 vs Rerun 10 Comparison

| Aspect                          | Rerun 8 (`814034f`)           | Rerun 9 (`b5db18b`)                      | Rerun 10 (`803e402`)                  |
|---------------------------------|-------------------------------|-------------------------------------------|----------------------------------------|
| Display mode                    | 1920x1080@60, VRR Never       | 1920x1080@60, VRR Never                   | 1920x1080@60, VRR Never                |
| capture.startStream             | ok=true                       | failed: no-acceptable-buffer-type         | **ok=true**                            |
| capture.sessionReady            | received                      | **missing** (regression)                  | **received**                           |
| no-acceptable-buffer-type       | absent                        | **present** (RetryShm took primary pod)   | **absent** (retry now uses legacy pod) |
| 60s diagnostics window reached  | yes                           | no (early failure)                        | **yes**                                |
| Diagnostics samples             | 60                            | 0                                         | **60**                                 |
| Encoder selected                | nvenc/h264                    | (not reached)                             | **nvenc/h264 (first-available)**       |
| Total frames produced           | 3252                          | 0                                         | 3324                                   |
| Total frames dropped            | 19 (all sample 1)             | n/a                                       | 19 (all sample 1)                      |
| Drop rate                       | 0.58 %                        | n/a                                       | 0.5716 %                               |
| Cadence mean (fps)              | 54.200                        | n/a                                       | **55.4**                               |
| FPS range (samples 2–60)        | 53–55                         | n/a                                       | 54–56                                  |
| VAL-CAP-004 verdict             | fail (cadence + drop)         | fail (startStream / sessionReady missing) | **fail (cadence + drop)**              |
| First failing gate              | cadence + drop                | startStream / no-acceptable-buffer-type   | cadence + drop                         |

Net effect: rerun 10 returns to the rerun 8 failure regime — capture pipeline is back up to the 60-second window, the regression introduced by the framerate-hint primary pod on the retry path is closed, and the residual failure is the pre-existing PipeWire ~54–56 fps delivery rate plus the startup drop burst.

## 7. capture.startStream Result

`startStream-response.json`:
```json
{ "jsonrpc": "2.0", "id": 2, "result": { "ok": true } }
```
**Pass.**

## 8. capture.sessionReady Result

`sessionReady-notification.json`:
```json
{
  "jsonrpc": "2.0",
  "method": "capture.sessionReady",
  "params": {
    "compositor_name": "pipewire",
    "format": {
      "color_primaries": "bt709",
      "fourcc": "XR24",
      "fps_den": 1,
      "fps_num": 0,
      "height": 1080,
      "transfer": "bt709",
      "width": 1920
    },
    "session_id": "pw-session-0000-183781-1779257928494",
    "stream_id": "pw-stream-0000"
  }
}
```
**Received.** Format is XR24 1920x1080, fps_num=0/fps_den=1 (variable rate) — same negotiation outcome as rerun 8.

## 9. no-acceptable-buffer-type Regression Verdict

**Gone.** The rerun-9 second-fire of "no more input formats" → second-fire-guard → no-acceptable-buffer-type does not appear in rerun 10. `capture.startStream` succeeded on first attempt and the stream proceeded to `capture.sessionReady`. The Path D fix (RetryShmAfterDmaBufFailure now uses `build_format_enum_pod_legacy_permissive`) is empirically confirmed to close the regression.

## 10. VAL-CAP-004 Verdict

**Fail** (key `1080p60-nvenc`, samples=60). Same threshold structure as rerun 8:
- `expectedEncoderBackend`: `nvenc`
- `nominalFps`: 60, `nominalSource`: `row-config`
- `maxDropRate`: 0
- `cadenceMeanToleranceFrac`: 0.005 (i.e. allowed range 59.70–60.30 fps)
- `declaredCaptureFormat`: 1920x1080
- `negotiatedCaptureFormat`: 1920x1080 XR24, fps_num=0/fps_den=1
- `cellMismatch`: false
- `encoderObserved`: nvenc

Per-gate results:

| Gate                                | Required             | Observed   | Pass |
|-------------------------------------|----------------------|------------|------|
| cell match declared                 | 1920x1080            | 1920x1080  | yes  |
| ≥1 diagnostics sample               | ≥1                   | 60         | yes  |
| encoder.selected backend = nvenc    | nvenc                | nvenc      | yes  |
| drop rate ≤ 0                       | ≤ 0                  | 0.005716   | **no** |
| cadence mean within ±0.5% of 60     | 59.70–60.30 fps      | 55.4       | **no** |

## 11. Diagnostics Sample Count

60 samples (one per second) — full 60-second observation window reached.

## 12. Encoder Selected / Backend Result

`encoder-selected.json`: backend=`nvenc`, codec=`h264`, reason_for_choice=`first-available`, parameters width=1920 height=1080 fourcc=XR24 fps_num=0 fps_den=1.

## 13. First Failing Gate

Two threshold gates fail simultaneously after the diagnostics window completes:
1. **drop rate** (0.5716 % vs required 0 %).
2. **cadence mean** (55.4 fps vs required 59.70–60.30 fps).

Neither is a startStream / sessionReady / encoder / cell-match failure. This is the rerun-8 failure family.

## 14. Was Cadence/Drop Evaluated?

Yes. The 60-second diagnostics window was re-entered after the rerun-9 regression closed, so cadence and drop rate were honestly computed from 60 samples / 3324 produced frames.

## 15. Did Cadence/Drop Pass or Fail?

Failed. See §10 and §13 — both cadence mean and drop rate are out of policy.

Comparison detail vs rerun 8 (no policy/threshold changes):
- Drop rate: rerun 8 0.58 % → rerun 10 0.5716 % (essentially identical; all drops in sample 1).
- Cadence mean: rerun 8 54.200 fps → rerun 10 55.4 fps (+1.2 fps).
- FPS histogram (samples 2–60), rerun 10: 56 fps × 22, 55 fps × 34, 54 fps × 3 (vs rerun 8: 53 × 4, 54 × 45, 55 × 10). Cadence is slightly higher in rerun 10 but still well below the 59.70 fps floor.
- The framerate-hint work (b5bfacfa / b5b…) appears to have nudged the steady-state delivery up by ~1 fps but did not bring the compositor to 60 fps.

## 16. Confirmation: No Source Files Changed

`git status --short --untracked-files=all`, both before and after the smoke run, lists only newly created evidence and handover files under `.story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-10/` and `.story/handovers/2026-05-19-10-t-021-mvp-smoke-rerun-10.md`. No tracked-file modifications and no untracked files outside that evidence/handover scope. No `helper/`, `src/`, `electron/`, `packaging/`, `.github/`, `validation/`, `package*.json`, `Cargo.toml`, or `Cargo.lock` changes. Runner output (`validation-artifacts/`, `validation-report-*.json`) is `.gitignore`d and does not appear in status.

## 17. Confirmation: No Validation Policy / Threshold Changes

`thresholds.json` for VAL-CAP-004 captured in evidence shows `maxDropRate: 0`, `cadenceMeanToleranceFrac: 0.005`, `expectedEncoderBackend: nvenc`, `nominalFps: 60`, `nominalSource: row-config`, `cellMismatch: false`. Identical to rerun 8 policy. No `validation/` files were edited.

## 18. Confirmation: No NVENC Changes

`helper/src/encoder/**`, NVENC FFI layout, NVENC probe code — untouched in this pass. Pre-existing NVENC snake-case warnings are the same warnings seen in earlier reruns.

## 19. Recommended Next Phase

Path D regression is closed and the smoke chain again terminates at the same cadence/drop wall first seen in rerun 7 and confirmed twice in rerun 8 and now rerun 10. Two independent reruns with VRR disabled, the new framerate hint applied, and (in rerun 10) the legacy-permissive pod also on the retry path still produce ~55 fps delivery from the compositor, with the startup drop burst confined to sample 1.

Recommended next pass (planning only — not started here):
- Treat the residual ~55 fps steady-state delivery as a **PipeWire / KDE compositor source-scheduler characteristic at this output**, not a helper bug, and decide policy at the validation layer:
  - Either (a) refine the VAL-CAP-004 cadence policy to honor the negotiated `fps_num=0` (variable rate) by using a tolerated steady-state window plus drift checks rather than a hard ±0.5% of nominal, or (b) explicitly classify ~55 fps as a host/compositor-class failure (matrix gate) that does not block the helper-correctness gate.
  - Treat the startup drop burst (19 frames in sample 1) as a startup transient and adopt a small startup-window exclusion / absolute-count tolerance (e.g. ≤ 25 drops in the first 2 s) in policy.
- Either decision requires a separate plan/review pass (cadence / variable-rate / matrix policy), which the rerun-10 prompt explicitly forbids — flagged here only.

T-021 remains **open**: VAL-CAP-004 has not passed. T-010c remains **blocked** on T-021.

---

## Evidence Index

All paths relative to `.story/handovers/evidence/2026-05-19-t-021-mvp-smoke-rerun-10/`:

- `preflight-head.txt`, `preflight-git-status.txt`, `preflight-git-log.txt`, `preflight-diff-check.txt`
- `build-sanity.txt`
- `display-mode-before.txt`, `display-mode-before.json`, `display-mode-after.txt`
- `nvidia-smi-before.txt`, `nvidia-smi-after.txt`, `nvidia-metamode-before.txt`
- `host-load-before.txt`, `host-load-after.txt`
- `portal-status-before.txt`
- `pgrep-cove-before.txt`, `pgrep-cove-after.txt`, `pgrep-ffmpeg-before.txt`, `pgrep-ffmpeg-after.txt`
- `runner-stdout.txt`, `runner-stderr.txt`
- `validation-report.json`
- `smoke-evidence-tree/report.json`
- `smoke-evidence-tree/VAL-CAP-003/*.json`
- `smoke-evidence-tree/VAL-CAP-004/*.json|*.txt`
