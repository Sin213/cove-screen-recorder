# NOTE: The single matching PID below is the evidence-collector shell itself; argv contains literal string "cove-replay-engine".
# This is the same known false positive documented in rerun 14. No real cove-replay-engine was running pre-run.
403947 /bin/bash -c source /home/sin/.claude/shell-snapshots/snapshot-bash-1779325698732-2qun4l.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && eval 'EVID=.story/handovers/evidence/2026-05-20-t-021-mvp-smoke-rerun-15 # Annotate the pgrep self-match cat > "$EVID/pgrep-cove-before.txt" <<EOF # NOTE: The single matching PID below is the evidence-collector shell itself; argv contains literal string "cove-replay-engine". # This is the same known false positive documented in rerun 14. No real cove-replay-engine was running pre-run. $(pgrep -af cove-replay-engine 2>&1 || echo none) EOF cat > "$EVID/pgrep-ffmpeg-before.txt" <<EOF # NOTE: Same self-match false positive; no real ffmpeg running pre-run. $(pgrep -af ffmpeg 2>&1 || echo none) EOF  # Switch display kscreen-doctor output.DP-4.mode.11 > "$EVID/display-switch.txt" 2>&1 sleep 1 kscreen-doctor -o 2>&1 > "$EVID/display-mode-after-switch.txt" echo "=== switch result ===" cat "$EVID/display-switch.txt" echo echo "=== current mode (DP-4 modes line) ===" grep -A1 "DP-4" "$EVID/display-mode-after-switch.txt" | grep -o '"'"'[0-9]*x[0-9]*@[0-9.]*\*'"'"' | head -3' && pwd -P >| /tmp/claude-4ebf-cwd
403979 /bin/bash -c source /home/sin/.claude/shell-snapshots/snapshot-bash-1779325698732-2qun4l.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && eval 'EVID=.story/handovers/evidence/2026-05-20-t-021-mvp-smoke-rerun-15 # Annotate the pgrep self-match cat > "$EVID/pgrep-cove-before.txt" <<EOF # NOTE: The single matching PID below is the evidence-collector shell itself; argv contains literal string "cove-replay-engine". # This is the same known false positive documented in rerun 14. No real cove-replay-engine was running pre-run. $(pgrep -af cove-replay-engine 2>&1 || echo none) EOF cat > "$EVID/pgrep-ffmpeg-before.txt" <<EOF # NOTE: Same self-match false positive; no real ffmpeg running pre-run. $(pgrep -af ffmpeg 2>&1 || echo none) EOF  # Switch display kscreen-doctor output.DP-4.mode.11 > "$EVID/display-switch.txt" 2>&1 sleep 1 kscreen-doctor -o 2>&1 > "$EVID/display-mode-after-switch.txt" echo "=== switch result ===" cat "$EVID/display-switch.txt" echo echo "=== current mode (DP-4 modes line) ===" grep -A1 "DP-4" "$EVID/display-mode-after-switch.txt" | grep -o '"'"'[0-9]*x[0-9]*@[0-9.]*\*'"'"' | head -3' && pwd -P >| /tmp/claude-4ebf-cwd
403980 /bin/bash -c source /home/sin/.claude/shell-snapshots/snapshot-bash-1779325698732-2qun4l.sh 2>/dev/null || true && shopt -u extglob 2>/dev/null || true && eval 'EVID=.story/handovers/evidence/2026-05-20-t-021-mvp-smoke-rerun-15 # Annotate the pgrep self-match cat > "$EVID/pgrep-cove-before.txt" <<EOF # NOTE: The single matching PID below is the evidence-collector shell itself; argv contains literal string "cove-replay-engine". # This is the same known false positive documented in rerun 14. No real cove-replay-engine was running pre-run. $(pgrep -af cove-replay-engine 2>&1 || echo none) EOF cat > "$EVID/pgrep-ffmpeg-before.txt" <<EOF # NOTE: Same self-match false positive; no real ffmpeg running pre-run. $(pgrep -af ffmpeg 2>&1 || echo none) EOF  # Switch display kscreen-doctor output.DP-4.mode.11 > "$EVID/display-switch.txt" 2>&1 sleep 1 kscreen-doctor -o 2>&1 > "$EVID/display-mode-after-switch.txt" echo "=== switch result ===" cat "$EVID/display-switch.txt" echo echo "=== current mode (DP-4 modes line) ===" grep -A1 "DP-4" "$EVID/display-mode-after-switch.txt" | grep -o '"'"'[0-9]*x[0-9]*@[0-9.]*\*'"'"' | head -3' && pwd -P >| /tmp/claude-4ebf-cwd
