# Sanitized pgrep output (redacted to remove unrelated shell command lines and session metadata)
# Original command: pgrep -af ffmpeg
# Captured: 2026-05-21 post-run

matchCount: 1
realFfmpegProcesses: 0
runnerOwnedFfmpegProcesses: 0
falsePositiveGrepMatches: 1   # bash command line containing the literal "ffmpeg" string
notes: |
  pgrep -af matched only the bash command line that invoked pgrep itself.
  No real ffmpeg process was running. The unrelated user-space ffmpeg
  observed pre-run had terminated. Combined with VAL-PROC-001/002/003
  evidence, this confirms zero runner-owned ffmpeg leak after the
  smoke run.
