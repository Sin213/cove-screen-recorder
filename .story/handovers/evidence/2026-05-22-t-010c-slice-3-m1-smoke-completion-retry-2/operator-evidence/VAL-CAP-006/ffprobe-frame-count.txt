# VAL-CAP-006 export file analysis
# File: /run/user/1000/cove-screen-recorder/exports/exp-1779476330387946665-0003.mp4
# Captured: 2026-05-22T18:58:50 UTC (= 11:58:50 PDT)
# NOTE: this is the v2 helper's runtime exports dir. The renderer's "Save last
# 0.5 min" landed the file here, not in ~/Videos/Cove Recordings/ (which holds
# only the discarded prior Region/v1 attempt files at 11:42:49 + 11:43:14).

-rw-r--r-- 1 sin sin 87122333 2026-05-22 11:58:50.664307449 -0700 /run/user/1000/cove-screen-recorder/exports/exp-1779476330387946665-0003.mp4

--- ffprobe stream summary (counted) ---
codec_name=h264
width=3840
height=2160
pix_fmt=yuv420p
r_frame_rate=120/1
avg_frame_rate=6750000/156691
duration=27.856178
bit_rate=25015845
nb_frames=1200
nb_read_frames=1200

--- ffprobe packets (cross-check) ---
nb_read_packets=1200

--- format container ---
format_name=mov,mp4,m4a,3gp,3g2,mj2
duration=27.856178
size=87122333
bit_rate=25020613

--- per-frame pict_type histogram (first 200 frames) ---
      2 I
    198 P

--- Interpretation (computed) ---
duration = 27.856178 s
declared_fps_per_ui_preset = 60 (from "Quality – 1440p · 60fps · 20 Mbps")
declared_fps_per_container_r_frame_rate = 120 (from container metadata)
nb_read_frames = 1200
avg_frame_rate_computed = 1200 / 27.856178 = 43.08 fps

Per N-008 line 409: "Frame count: round(duration × declared_fps) per 6.1".
At declared_fps=60: expected = round(27.856 × 60) = 1671  → 1200 / 1671 = 71.8 % (= 28 % deficit vs. nominal)
At declared_fps=120: expected = round(27.856 × 120) = 3343 → 1200 / 3343 = 35.9 % (= 64 % deficit vs. nominal)

VAL-CAP-006 criterion text: "declared frame count produced even while source
window is minimised or behind another window for the full 60 s; HUD timer
increments". Verdict per Codex 2026-05-22T16:43:28 Issue #1: **leg 1 FAILS**
(1200 actual vs 1671 declared = 28 % deficit). The Issue #3 absorption
"frames continue while occluded" sub-property is partially demonstrated (some
frames produced; no DOM/hover dependency observed), but the row's plain pass
text requires the declared count, not "any frames produced". The fps deficit
is also not a fake-duplicated-frame regression (that would be VAL-REG-002 /
VAL-EXP-010); the helper is emitting REAL cadence per VAL-EXP-011's spirit
("real cadence preserved; sub-60 fps source faithfully reproduced, not
padded to fake 60"), but real-cadence preservation does not satisfy
VAL-CAP-006's declared-count criterion. The deficit is consistent with
KWin's PipeWire
screencast behavior: under SHM fallback (DMA-BUF rejected; see helper log
17:58:50.272Z + 18:54:27.103Z + 18:57:17.648Z) and with the source compositor
substantially idle (Cove occluded for ≥60 s of the recording window), KWin
delivers frames at the compositor's actual paint rate, not nominal 60 Hz.

**This DOES block VAL-CAP-006's leg 1.** Per Codex review 2026-05-22T16:43:28
(Issue #1), the row's plain pass text ("declared frame count produced") binds
the verdict to the declared-count rule, not to VAL-EXP-011's real-cadence
rule. With the declared-count rule, this is a FAIL: 1200 actual vs 1671
declared = 28 % deficit. The real-cadence framing above (paragraphs 1-3 of
this Interpretation block) explains the *physical* cause of the deficit
(SHM fallback + occluded compositor) and demonstrates the absence of fake-
frame padding (VAL-REG-002 not regressed), but it does not satisfy the row
criterion. Owner-on-fail layer per N-008 §3: capture / encoder.

--- Deviations from row matrix expectations (logged for Codex review) ---
1. Source resolution: row matrix says "1080p60", actual captured frames are
   3840x2160. DP-4 was kscreen-doctor-set to 1920x1080@60 in pre-flight, but
   KDE auto-restored its preferred 3840x2160@240 mode at some point during
   the session (kscreen-doctor confirms post-row DP-4 is back at
   3840x2160@240). No "capture.formatChanged" event in the helper slice, so
   the reversion likely happened before any capture started OR happened
   silently between sessions. The N-008 criterion text for VAL-CAP-006 is
   resolution-independent (HUD continuity + frame production), so the
   resolution drift by itself does not change this row's verdict — but the
   verdict is **BLOCKED** regardless because leg 1 (declared frame count
   produced) fails on the count arithmetic above (1200 vs 1671 = 28 %
   deficit), independent of source resolution. The drift is a kscreen /
   KDE-Plasma side effect, not a v2 capture defect. It is plausibly
   contributory to the frame-count deficit (4K under SHM may explain the
   shortfall) — ISS-011 retests at confirmed 1080p60 to disambiguate.

2. Two distinct PipeWire sessions in this row's slice (18:54:27 + 18:57:17).
   Operator clicked Start replay buffer ONCE per their report. The second
   portal-session at 18:57:17 has no explanation from the renderer side; it
   may be a helper-internal segment-roll behavior or an inadvertent UI
   interaction during the coverage maneuver. Both sessions emit clean
   "PW stream ready" within ≤25 ms of "portal session established", so the
   v2 capture path is functioning. Worth a follow-up investigation but
   does not invalidate VAL-CAP-006.

3. Export landed at the helper runtime exports dir
   (/run/user/1000/cove-screen-recorder/exports/) rather than the
   user-visible ~/Videos/Cove Recordings/. This appears to be the documented
   v2 path output location; the renderer Save UI does not appear to surface
   the file in the user's home Videos folder. Whether that's a v2 path
   choice or a missing copy-to-Videos step is a separate UI concern,
   unrelated to VAL-CAP-006.
