=== Post-run process check (exact name match, pgrep -x) ===
Timestamp: 2026-05-18T23:34:59Z

Method: pgrep -x <name> (exact executable name — avoids self-match from bash subprocesses
        that contain the target name in their command line arguments)

cove-replay-engine: none
ffmpeg: none
pactl: none

All target processes absent — clean

Note: pgrep -f (full-cmdline match) was initially used but produced false positives from
the bash subprocess running the check command itself (the cmdline string contained the
pattern being searched). Replaced with pgrep -x (exact name) to give a clean, self-excluding
result. The helper PID 489829 was confirmed stopped before the suite completed.
