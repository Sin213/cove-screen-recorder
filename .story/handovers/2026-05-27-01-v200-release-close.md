# v2.0.0 Release — Project Close

**Date:** 2026-05-27
**Ticket:** T-010e → complete
**HEAD:** `bba295e` (pushed to origin/main)

## Session Work

This session completed the final v2.0.0 release work:

### Crop overlay compositor fix
- Replaced clip-path polygon dim layer in CropOverlay.tsx with four deterministic absolute-positioned rectangles (top/bottom/left/right)
- Eliminates dark vertical bands under Wayland Ozone GPU compositor

### Wayland double-prompt fix
- Removed CropOverlay from the Wayland recording flow entirely (both GIF hotkey and regular crop mode)
- On Wayland, PipeWire portal grant now starts recording immediately — one step instead of two
- Credit: reported and suggested by [@Kraibse](https://github.com/Kraibse)

### Balanced preset resolution fix
- Added `captureQuality` to the Wayland direct-start path so balanced preset properly downscales to 1080p instead of recording at native 1440p

### Compositor settle delay
- Added rAF + 200ms delay between PipeWire stream acquisition and MediaRecorder start to prevent portal overlay from appearing in first GIF frames

### Release documentation
- README updated for v2.0.0: version strings, removed all Windows references (Linux-only), added replay buffer feature, updated Wayland notes, added Kraibse acknowledgement
- CHANGELOG updated with all new fixes and Kraibse credit
- GitHub release draft (v2.0.0) updated with complete notes

## Commits pushed (6)
- `e567a5e` Fix crop overlay Wayland rendering and GIF hotkey double-selection
- `bb14312` Skip CropOverlay on Wayland for all crop modes
- `2c7b659` Pass captureQuality in Wayland direct-start path
- `20cf6bc` Add compositor settle delay to Wayland direct-start path
- `bba295e` Update README and CHANGELOG for v2.0.0 release

## Project State
- All 47/47 tickets complete
- GitHub release draft ready at Sin213/cove-screen-recorder (v2.0.0, draft)
- Release artifacts uploaded: AppImage + deb + sha256 sidecars
- Project is closed for v2.0.0; v3.0.0 will start as a new story
