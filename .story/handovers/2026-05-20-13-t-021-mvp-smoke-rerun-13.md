# T-021 MVP Smoke Rerun 13 — VAL-CAP-004 Cadence Warmup Scope Fix Evidence

**Date:** 2026-05-20
**Pass ID:** T-021 MVP smoke rerun 13
**Commit tested:** e2d575c (Fix VAL-CAP-004 cadence warmup scope)
**Branch:** main
**Evidence directory:** `.story/handovers/evidence/2026-05-20-t-021-mvp-smoke-rerun-13/`
**Validation report:** `.story/handovers/evidence/2026-05-20-t-021-mvp-smoke-rerun-13/validation-report.json`

---

## 1. Repo / Commit Tested

```
HEAD: e2d575cb913d8f0e72b507c837815f691610aac4
Log:
e2d575c Fix VAL-CAP-004 cadence warmup scope         ← tested
1551587 Handle VAL-CAP-004 variable-rate cadence
a893ba2 Record MVP smoke rerun 11 evidence
b4bdb9f Handle VAL-CAP-004 startup drop warmup
a519115 Record MVP smoke rerun 10 evidence
803e402 Fix PipeWire SHM retry framerate fallback
```

Working tree: **clean** (no tracked modifications, no staged changes)
`git diff --check`: exit 0

**Note on preflight-git-status.txt:** The captured `preflight-git-status.txt` shows untracked entries (evidence files created before the git-status capture in the same shell invocation). These are capture-sequencing artifacts, not pre-existing modifications. No tracked files were modified or staged at any point during this run.

---

## 2. Preflight Result

| Check | Result |
|---|---|
| `git status --short` | no tracked/staged changes (untracked evidence files are capture artifacts) |
| `git diff --check` | exit 0 |
| HEAD | e2d575c Fix VAL-CAP-004 cadence warmup scope |
| Required commits present | e2d575c, 1551587, a893ba2, b4bdb9f — all confirmed |

All preflight conditions satisfied.

---

## 3. Environment Summary

- **Session:** KDE Wayland, WAYLAND_DISPLAY=wayland-0
- **Display:** DP-4, 1920x1080@60.00 (mode 11, active)
- **VRR:** Never
- **GPU:** RTX 4080 SUPER, Driver 595.71.05, CUDA 13.2
- **GPU state before:** 41°C, 19.62W, 17% util, 4605MiB/16376MiB
- **GPU state after:** 47°C, 17.10W, 3% util, 4435MiB/16376MiB
- **Portal:** xdg-desktop-portal active, xdg-desktop-portal-kde via D-Bus
- **Stale processes:** None (pgrep cove-replay-engine: exit 1, pgrep ffmpeg: exit 1)
- **Host load before:** 0.84, 0.84, 0.67
- **Host load after:** 1.10, 0.95, 0.74

---

## 4. Exact Commands Run

**Preflight:**
```bash
git rev-parse HEAD
git status --short --untracked-files=all
git log --oneline -10
git diff --check
```

**Sanity:**
```bash
npm run typecheck                                           # pass
npm run validate:build                                      # pass
node --test dist-validation/drivers.drop-warmup.test.js     # 7/7 pass
node --test dist-validation/drivers.cadence-policy.test.js  # 10/10 pass
npm run build                                               # pass
cargo build -p cove-replay-engine --release                 # pass (NVENC naming warnings only)
```

**Display switch:**
```bash
kscreen-doctor output.DP-4.mode.11   # → 1920x1080@60.00
```

**Evidence collection:**
```bash
kscreen-doctor --outputs       # before/after
kscreen-doctor --json          # before
nvidia-smi                     # before/after
ps aux --sort=-%cpu | head -15 # before/after
pgrep -af cove-replay          # before/after
pgrep -a ffmpeg                # before/after
```

**Smoke run:**
```bash
RUST_LOG=info,cove_replay_engine=debug \
  node dist-validation/runner.js smoke \
  > .story/handovers/evidence/2026-05-20-t-021-mvp-smoke-rerun-13/runner-stdout.txt \
  2> .story/handovers/evidence/2026-05-20-t-021-mvp-smoke-rerun-13/runner-stderr.txt
```

**Portal:** Operator selected DP-4, clicked Share promptly.

---

## 5. Evidence Directory

```
.story/handovers/evidence/2026-05-20-t-021-mvp-smoke-rerun-13/
  preflight-head.txt
  preflight-git-status.txt
  preflight-git-log.txt
  preflight-diff-check.txt
  build-sanity.txt
  display-mode-before.txt
  display-mode-before.json
  display-mode-after.txt
  nvidia-smi-before.txt
  nvidia-smi-after.txt
  host-load-before.txt
  host-load-after.txt
  pgrep-cove-before.txt
  pgrep-cove-after.txt
  pgrep-ffmpeg-before.txt
  pgrep-ffmpeg-after.txt
  runner-stdout.txt
  runner-stderr.txt
  validation-report.json
  smoke-evidence-tree/
    report.json
    VAL-CAP-003/
    VAL-CAP-004/
      thresholds.json
      drop-warmup.json
      capture-diagnostics.json
      sessionReady-notification.json
      startStream-response.json
      encoder-selected.json
      encoder-probe-result.json
      env-probe.json
      engine-ready.json
      load-launch.json
      requestSession-response.json
      stopSession-response.json
      shutdown-response.json
    VAL-ENC-001/
    VAL-SEG-001/
    VAL-SEG-003/
```

---

## 6. Rerun 12 vs Rerun 13 Comparison

| Metric | Rerun 12 (1551587) | Rerun 13 (e2d575c) | Delta |
|--------|---------------------|---------------------|-------|
| Commit | Handle VAL-CAP-004 variable-rate cadence | Fix VAL-CAP-004 cadence warmup scope | warmup scope fix |
| cadencePolicy | variable-rate | variable-rate | same |
| Drop gate | PASS | PASS | same |
| dropWarmupSamples | 1 | 1 | same |
| effectiveSamples | 59 | 59 | same |
| dropRate | 0.000 | 0.000 | same |
| excludedDropped | 20 | 20 | same |
| Cadence stats scope | all samples (bug) | effective post-warmup (fixed) | **FIXED** |
| Reported meanFps | 54.983 (all-sample) | 54.746 (effective) | corrected scope |
| Reported spreadFps | 7.000 (all-sample) | 2.000 (effective) | **corrected scope** |
| Mean gate [51..61.2] | PASS | PASS | same |
| **Spread gate ≤ 6.0** | **FAIL (7.000)** | **PASS (2.000)** | **FIXED** |
| Strict ±0.5% gate | informational FAIL | informational FAIL | same (not gating) |
| warmup sample 0 fps | 61 | 60 | minor variance |
| effective min fps | 54 | 54 | same |
| effective max fps | 56 | 56 | same |
| samples | 60 | 60 | same |
| encoder | nvenc h264 | nvenc h264 | same |
| **VAL-CAP-004 verdict** | **FAIL** | **PASS** | **GREEN** |

---

## 7. capture.startStream Result

**PASS** — `startStream-response.json`: `{"result": {"ok": true}}`

---

## 8. capture.sessionReady Result

**PASS** — `sessionReady-notification.json`:
- compositor_name: pipewire
- format: 1920x1080, fps_num=0, fps_den=1 (variable-rate)
- session_id: pw-session-0000-275939-1779293567990

---

## 9. no-acceptable-buffer-type Verdict

**GONE** — Runner stderr shows no `no-acceptable-buffer-type` error. Session reached sessionReady and completed full 60-second diagnostics window.

---

## 10. dropWarmupSamples Evidence

From `drop-warmup.json`:
```json
{
  "rowDropWarmupSamples": 1,
  "warmupSamples": 1,
  "totalSamples": 60,
  "effectiveSamples": 59,
  "totalDropped": 0,
  "dropWarmupExcludedDropped": 20,
  "dropWarmupProduced": 60,
  "dropEffectiveProduced": 3230,
  "dropRate": 0,
  "valid": true,
  "passed": true
}
```

Drop gate **PASSES**: warmup=1 excludes sample 0 (20 startup drops). Effective drop rate = 0/3230 = 0.000.

---

## 11. cadencePolicy Evidence

From `thresholds.json`:
```json
{
  "nominalFps": 60,
  "nominalSource": "row-config",
  "cadencePolicy": "variable-rate",
  "variableRateCadenceConstants": {
    "variableRateCadenceMinFracOfNominal": 0.85,
    "variableRateCadenceMaxFracOfNominal": 1.02,
    "variableRateCadenceMaxSpreadFps": 6
  }
}
```

`fps_num=0` correctly triggers `cadencePolicy=variable-rate`.

---

## 12. Cadence Warmup-Scope Verdict

**FIXED.** The cadence warmup scope fix (e2d575c) correctly applies `dropWarmupSamples=1` exclusion to cadence statistics.

| Stat | Rerun 12 (bug) | Rerun 13 (fix) |
|---|---|---|
| Cadence input scope | all 60 samples | effective 59 samples (post-warmup) |
| meanFps | 54.983 (all-sample) | 54.746 (effective) |
| spreadFps | 7.000 (all-sample) | 2.000 (effective) |
| Spread gate ≤ 6.0 | FAIL | **PASS** |

FPS distribution (effective post-warmup samples):
- 54 fps: 16 samples
- 55 fps: 42 samples
- 56 fps: 1 sample
- Effective mean: 54.746 fps
- Effective spread: 2 fps (56 - 54)

Warmup sample 0 fps: 60 (in rerun 12 it was 61; variance is normal for startup burst).

---

## 13. Strict 60fps Informational Evidence

From validation report:
```json
{
  "name": "cadence mean within ±0.5% of 60.00 fps (nominal source=row-config, informational)",
  "observed": "54.746",
  "required": "59.70..60.30",
  "passed": false,
  "gating": false
}
```

Strict ±0.5% gate is present, informational (`gating: false`), and correctly not blocking. **Confirmed demoted to informational.**

---

## 14. Drop-Rate Verdict

**PASS** — drop rate = 0.000 (effective 59 samples, 3230 frames, 20 startup drops excluded by warmup=1).

---

## 15. Cadence Verdict

**PASS** — All gating cadence gates pass.

| Gate | Observed | Required | Passed |
|---|---|---|---|
| Mean in [51.00..61.20] fps | 54.746 | 51.00..61.20 | PASS |
| Spread ≤ 6.0 fps | 2.000 | ≤ 6.0 | PASS |
| Strict ±0.5% (informational) | 54.746 | 59.70..60.30 | FAIL (not gating) |

---

## 16. VAL-CAP-004 Verdict

**PASS — GREEN**

| Gate | Status |
|---|---|
| Cell match (1920x1080) | PASS |
| Drop rate ≤ 0 | PASS (0.000) |
| Cadence mean [51.00..61.20] fps | PASS (54.746) |
| Cadence spread ≤ 6.0 fps | PASS (2.000) |
| Strict ±0.5% cadence (informational) | FAIL (not gating) |
| At least 1 sample | PASS (60) |
| Encoder backend nvenc | PASS |

---

## 17. Diagnostics Sample Count

- Total samples collected: **60**
- Effective samples (post-warmup): **59**
- Total frames produced: **3290** (including warmup)
- Effective frames produced: **3230**
- Warmup excluded dropped: **20**
- 60-second window: **complete**

---

## 18. Encoder Selected / Backend

```json
{
  "backend": "nvenc",
  "codec": "h264",
  "reason_for_choice": "first-available"
}
```

encoder.selected fired. Backend: **nvenc (h264)**. libx264 unavailable (not-implemented-yet, T-017a).

---

## 19. First Failing Gate (in suite, not VAL-CAP-004)

VAL-CAP-004 itself: **no failing gates** (all gating thresholds pass).

Suite stopped at **VAL-SEG-003** (replay.save latency gate):
```
replay.save response: {"code":-32600,"message":"no committed segments available to pin"}
```
This is a segment-layer issue unrelated to VAL-CAP-004 capture/cadence. VAL-SEG-003 is owned by rolling-buffer (T-018/T-019 scope), not capture.

---

## 20. Recommended Next Phase

**VAL-CAP-004 is GREEN. T-021 capture validation is complete.**

The cadence warmup scope fix chain (b4bdb9f → 1551587 → e2d575c) resolves all VAL-CAP-004 gating failures across reruns 10–13:
- Rerun 10: drop rate FAIL → fixed by b4bdb9f (startup drop warmup)
- Rerun 11: cadence mean FAIL (strict) → fixed by 1551587 (variable-rate policy)
- Rerun 12: cadence spread FAIL (7 > 6.0) → fixed by e2d575c (warmup scope)
- **Rerun 13: ALL GATING GATES PASS**

VAL-SEG-003 (replay.save "no committed segments") is the next blocking row. It should be filed as an issue against rolling-buffer and addressed before T-021 can fully close and unblock T-010c.

### Recommended sequence:
1. File ISS for VAL-SEG-003 against rolling-buffer
2. Fix VAL-SEG-003 in scope of T-018/T-019
3. Rerun smoke to confirm full suite green
4. Close T-021, unblock T-010c

---

## Confirmation: No Source/Validation Changes During Rerun

- **No changes to `helper/`** — confirmed, no tracked/staged changes throughout
- **No changes to `validation/`** — confirmed
- **No changes to `electron/`** — confirmed
- **No changes to `src/`** — confirmed
- **VAL-CAP-004 thresholds unchanged** — maxDropRate=0, cadenceMeanToleranceFrac=0.005, variableRateCadenceMaxSpreadFps=6
- **No NVENC/encoder changes** — confirmed
- **T-010c NOT started** — deferred per task discipline
