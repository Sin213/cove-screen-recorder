# Handover — v2.0.0 Storybloq bootstrap

**Date:** 2026-05-13
**Session type:** Storybloq bootstrap only (planning, no code)
**Repo:** `/home/sin/Projects/cove-screen-recorder`
**Current release:** v1.1.0 (stable, tagged, pushed)

---

## What this session did

Initialized `.story/` as the repo memory source of truth and committed v2.0.0 planning to disk. **No source files were changed.**

Files created:

- `.story/.gitignore` (created by `storybloq_init`)
- `.story/config.json` (created by `storybloq_init`)
- `.story/roadmap.json` (created by `storybloq_init`, then phases appended)
- `.story/project-state.md`
- `.story/tickets/T-001.json` … `T-010.json`
- `.story/handovers/2026-05-13-01-v2-bootstrap.md` (this file)
- `.story/notes/N-001.json` (MediaRecorder ceiling rationale)

Phases created (in order):

1. `p0-maintenance` — v1.1.1 Optional Maintenance
2. `p1-research` — v2.0.0 Architecture Research and Boundary
3. `p2-design` — v2.0.0 Component Design
4. `p3-integration` — v2.0.0 Integration and Validation Planning
5. `p4-release` — v2.0.0 Release

Tickets created:

- **T-001** Bootstrap v2.0.0 Storybloq planning and architecture boundary → `p1-research`
- **T-002** Optional v1.1.1 maintenance: replay keybind + timer state cleanup → `p0-maintenance`
- **T-003** Research and choose native replay engine approach → `p1-research`
- **T-004** Design PipeWire capture backend boundary → `p2-design`
- **T-005** Design encoder backend matrix and fallback policy → `p2-design`
- **T-006** Design rolling replay segment buffer → `p2-design`
- **T-007** Design instant replay export/remux pipeline → `p2-design`
- **T-008** Plan Electron UI integration with native/helper engine → `p3-integration`
- **T-009** Plan validation matrix for 1080p60, 1440p60, and 4K60 → `p3-integration`
- **T-010** v2.0.0 release checklist → `p4-release`

---

## What the next Claude session should do

**Read first**, in this order:

1. `CLAUDE.md` (repo rules)
2. `.story/project-state.md` (current state, ceiling, direction, issue triage)
3. `WORKFLOW.md` and `RELEASES.md` (local workflow + release rules)
4. This handover

**Then, before any work:**

- Ask the user which path to take:
  - **Path A:** ship v1.1.1 maintenance first (T-002), then start v2.
  - **Path B:** skip v1.1.1 entirely and go straight to v2 starting at T-003.
- If the user picks Path A, T-002 is strictly scoped — replay keybind colocation + crop/replay timer state UI cleanup only. Do **not** touch MediaRecorder, capture pipeline, or encoder logic in v1.1.1.
- If the user picks Path B, mark T-002 deferred and move directly to T-003.

**For v2 planning work (T-003 → T-009):**

- These are design tickets, not implementation tickets. Output is design docs / decision records, captured in `.story/notes/` or a `docs/` path the user approves. No source code, no package changes, no build/CI changes.
- Each design must explicitly carry forward the v1.1.0 fixes listed in `project-state.md` so they cannot regress in v2.

**For T-010 (release):**

- Do not run release commands until the user explicitly asks. The ticket exists so the checklist is ready; execution is gated on the user.

---

## Hard rules carried into v2

From `CLAUDE.md` and global instructions:

- Do not build, package, release, publish, tag, or commit unless explicitly asked.
- Treat this repo as the only source of truth. Do not borrow patterns from sibling projects.
- Keep changes surgical and task-scoped.
- If unsure, say **unknown** — do not invent project behavior.
- Every shipped artifact gets a `.sha256` sidecar (global rule).

From v1.1.0 lessons:

- On Linux, when in doubt on hardware encoder paths during replay finalization, use `libx264`. v1.1.0 explicitly bypasses broken hardware encoder chains for replay finalization.
- Replay save must never freeze the capture pipeline.
- No leftover `ffmpeg` or `pactl` processes after stop.
- No fake duplicated-frame 60fps output.
- Audio sidecar muxing must remain stable.

---

## Issue triage carried forward

| Issue | Disposition | Owning ticket |
| ----- | ----------- | ------------- |
| #1 crop area selection unclear/missing | Fold into v2 | T-004 |
| #2 replay keybind not configurable/colocated | v1.1.1 candidate, else absorbed by T-008 | T-002 (preferred), T-008 (fallback) |
| #3 source does not record until hovered | Fold into v2 | T-004, T-008 |
| #4 crop/replay timer state weirdness | v1.1.1 candidate (UI-state only), else absorbed by T-008 | T-002 (preferred), T-008 (fallback) |

---

## Intentionally not implemented in this session

- No source file edits.
- No package.json / lockfile / build config changes.
- No Electron main or renderer changes.
- No test changes.
- No replay engine prototype.
- No PipeWire / NVENC / VAAPI / QSV / AMF probe code.
- No commits, no tags, no pushes.

The next session starts cleanly from this handover plus `project-state.md`.
