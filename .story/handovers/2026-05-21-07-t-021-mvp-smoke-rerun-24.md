# T-021 MVP Smoke Rerun 24 — VAL-EXP-010 verification attempt

- Date (local): 2026-05-21 14:57–15:00
- Repo: /home/sin/Projects/cove-screen-recorder
- Commit tested: f352ccca8cedf22be1af8bd083f9ee5d55bff649
- Pass id: T-021 rerun 24 / VAL-EXP-010 verification
- Evidence dir: `.story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-24/`
- Runner artifact: `validation-artifacts/smoke/2026-05-21T21-57-54-390Z/`

## Result summary

- Suite verdict: **FAIL**
- Pass / fail / skip / error: **1 / 1 / 2 / 0**
- Stopped early at: **VAL-CAP-004**
- First failing gate: **VAL-CAP-004 — cadence spread**
- VAL-EXP-010: **NOT REACHED**

## Gate verdicts

| Row         | Status | Notes |
|-------------|--------|-------|
| VAL-PKG-001 | skip   | `helper-not-available` (runner launches helper later for scripted rows; same as prior reruns) |
| VAL-CAP-001 | skip   | `manual` |
| VAL-CAP-003 | pass   | Portal denial emitted `capture.sessionLost(portal-denied)`; helper returned to IDLE in 156 ms |
| VAL-CAP-004 | **fail** | Cadence quality gate tripped on **spread = 15.000 fps** (limit ≤ 6.0); see thresholds below |
| VAL-SEG-003 | not reached | runner stopped at VAL-CAP-004 |
| VAL-EXP-001 | not reached | runner stopped at VAL-CAP-004 |
| VAL-EXP-010 | **not reached** | runner stopped at VAL-CAP-004 |
| VAL-REG-002 | not reached | runner stopped at VAL-CAP-004 |

## VAL-CAP-004 detail

Key (`thresholds.json`): `1080p60-nvenc`, variable-rate policy, `nominalFps=60`, `cadencePolicy=variable-rate`, `variableRateCadenceMaxSpreadFps=6`.

| Threshold | Observed | Required | Pass |
|-----------|----------|----------|------|
| capture cell matches declared (1920x1080) | 1920x1080 | 1920x1080 | ✓ |
| drop rate ≤ 0 | 0.000 (warmup=1, effectiveSamples=59, excludedDropped=17, dropEffectiveProduced=3287) | ≤ 0 | ✓ |
| cadence mean in variable-rate range [51.00..61.20] fps | 55.712 | 51.00..61.20 | ✓ |
| **cadence spread ≤ 6.0 fps (variable-rate compositor variance)** | **15.000** | **≤ 6.0** | **✗ (gating)** |
| cadence mean within ±0.5% of 60.00 fps (informational) | 55.712 | 59.70..60.30 | ✗ (informational only) |
| at least 1 diagnostics sample received | 60 | ≥ 1 | ✓ |
| encoder.selected backend is nvenc | nvenc | nvenc | ✓ |

Encoder selected: NVENC h.264 (`encoder-selected.json`); `encoder-probeResult.json` shows libx264 unavailable (`reason=not-implemented-yet`, follow-up T-017a) — expected.

Capture session: `pw-session-0000-675650-1779400717058`, compositor `pipewire`, format XR24 1920x1080 (fps_num=0/fps_den=1 — compositor leaves fps unspecified).

### Cadence analysis (computed from `capture-diagnostics.json`, 60 samples, 1 warmup excluded)

- min = 54, max = 69, **spread = 15**
- mean (post-warmup) = **55.712**
- p50 = 55, p95 = 69, p99 = 69
- samples ≥ 60 fps: indices 44 (61), 45 (65), 46 (69), 50 (69), 51 (69) — two clustered bursts inside the ~44–51 s window
- baseline observedFps stays at 54–55 for the rest of the 60-sample window
- `droppedSinceLast` is 0 for all samples after warmup; total `totalDropped=0`

Interpretation: the compositor is delivering a sustained sub-nominal cadence (54–55 fps on a 60 Hz monitor) punctuated by short bursts where buffered frames flush at 65–69 fps. Drops are zero, so frames are not being lost; they are being delivered with non-uniform timing. The variable-rate spread tolerance (`6 fps`) is tripped by these compositor bursts.

The capture cadence is **upstream of the NVENC PTS fix** (`be2b653 Use real capture timestamps for NVENC fragments`). That fix governs how encoder fragment PTS values are derived from capture timestamps; it does not influence the cadence of the underlying PipeWire compositor delivery.

## VAL-EXP-010 verdict

**Not reached** — runner stopped at VAL-CAP-004. VAL-EXP-010 verification deferred until VAL-CAP-004 produces a clean run.

## Environment

- HEAD: `f352ccca` (clean tree at start, `git diff --check` clean)
- Recent commits present per spec (rerun-23 evidence, rerun-22 evidence, real PTS fix, rerun-21 evidence, ffmpeg muxer fix)
- DP-4: configured to **1920x1080@60.00**, VRR Never, HDR enabled, Wide Color Gamut enabled (pre-modeset snapshot shows DP-4 at 3840x2160@239.99; `kscreen-doctor output.DP-4.mode.1920x1080@60` applied before run — see `kscreen-doctor-pre-before-modeset.{txt,json}` vs `kscreen-doctor-pre.{txt,json}`)
- GPU: NVIDIA RTX 4080 SUPER, driver 595.71.05, CUDA 13.2. Observed utilization: **20% pre / 3% post**; memory used 4311 MiB pre → 4112 MiB post; perf state P8 (lowest). Active GPU clients pre-run include `mpv` (C+G, 942 MiB — actively decoding), `vesktop` (G, 1098 MiB), `kwin_wayland` (G, 475 MiB), `plasmashell` (G, 295 MiB), plus the usual KDE/Firefox/Dolphin set. Post-run shows `mpv` still attached at 854 MiB (C+G) and `vesktop` at 1098 MiB. The 20% pre-run GPU-util and the C+G mpv client are flagged here because they sit upstream of the KWin compositor that fed VAL-CAP-004; whether they materially affect the cadence spread is one of the open questions for follow-up (see Recommendations).
- Uptime: 2d 5h 45m pre → 2d 5h 48m post; system load 0.74 → 0.76 (15 min avg).
- `pgrep` files capture the shell command itself (the literal string `cove-replay-engine` appears in the bash command line); no real cove or ffmpeg binaries are running pre or post — verified separately. Same artifact pattern as prior rerun evidence; confirms no cove/ffmpeg process leak, but does not by itself establish a quiesced compositor environment.

## Build / test sanity (all green)

```
cargo build -p cove-replay-engine --release   → 0 errors, 70 warnings
cargo test -p cove-replay-engine --lib        → 101 passed
cargo test -p cove-replay-engine --test encoder_session → 28 passed
cargo test -p cove-replay-engine --test segment_buffer  →  7 passed
npm run typecheck                             → ok
npm run validate:build                        → ok
npm run build                                 → ok (vite renderer + tsc electron)
```

## Recommendations

- **ISS-006**: **Do not close.** Cadence spread now reproduces deterministically on KWin/PipeWire/Wayland under the rerun-24 conditions. Update ISS-006 with rerun-24 evidence: spread = 15 fps, mean = 55.7 fps on a 60 Hz monitor, no drops. The portal-timeout symptom from reruns 22/23 has been replaced by a real capture-cadence symptom; the underlying compositor variance is now observable.
- **VAL-EXP-010**: Status unchanged from rerun-21 conclusion (first failing gate). Cannot re-verify until VAL-CAP-004 passes. The NVENC real-timestamp fix from `be2b653` remains correct in code but its end-to-end impact on VAL-EXP-010 is unverified.
- **T-021**: **Cannot green.** First failing gate (VAL-CAP-004) blocks suite completion.
- **T-010c**: **Stays blocked** behind T-021.
- **Next recommended phase**: investigate the source of VAL-CAP-004 cadence spread. Decision needed between:
  1. **Source-side**: chase the KWin/PipeWire compositor variance (driver/kernel, refresh-rate negotiation, KWin compositor settings, monitor link state). The 54–55 fps sustained pattern with 65–69 fps bursts is consistent with a 60 Hz vsync clock that misses a vblank every ~16th frame and then catches up.
  2. **Validation policy**: re-examine whether `variableRateCadenceMaxSpreadFps = 6` is realistic for KWin/PipeWire delivery under variable-rate cadence. If 15-fps spread is intrinsic to KWin/PipeWire on this hardware, the threshold may be too tight — but that is a policy change that must be justified separately, not a rerun patch.
  3. **Operator/environment**: rule out background compositor activity (overlays, focus changes, notifications, KWin animations) during the 60 s capture by repeating under a quiesced session. Note in particular that the rerun-24 environment had `mpv` actively decoding (C+G client, 942 MiB) and `vesktop` resident (1098 MiB) on the same GPU, and pre-run GPU-Util was 20%. Repeating with these clients exited would isolate compositor variance from GPU contention.

No source / validation / policy changes were made during this rerun.

## Confirmation

- No edits to `helper/**`, `validation/**`, `electron/**`, `src/**`, `packaging/**`, `.github/**`, `package.json`, `package-lock.json`, `Cargo.toml`, `Cargo.lock`, `.story/tickets/T-021.json`, `.story/tickets/T-010c.json`, or `.story/issues/**`.
- Only allowed writes were under `.story/handovers/2026-05-21-07-t-021-mvp-smoke-rerun-24.md`, `.story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-24/**`, and `/home/sin/Desktop/Claude-Handoff/handoff.md`.

## Commands run

```
git rev-parse HEAD
git status --short --untracked-files=all
git log --oneline -12
git diff --check

cargo build -p cove-replay-engine --release
cargo test -p cove-replay-engine --lib
cargo test -p cove-replay-engine --test encoder_session
cargo test -p cove-replay-engine --test segment_buffer
npm run typecheck
npm run validate:build
npm run build

mkdir -p .story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-24
kscreen-doctor -o > …/kscreen-doctor-pre.txt          (before modeset → renamed to *-before-modeset)
kscreen-doctor -j > …/kscreen-doctor-pre.json         (before modeset → renamed to *-before-modeset)
kscreen-doctor output.DP-4.mode.1920x1080@60          (mode change)
kscreen-doctor -o > …/kscreen-doctor-pre.txt          (post-modeset, treated as run baseline)
kscreen-doctor -j > …/kscreen-doctor-pre.json
nvidia-smi > …/nvidia-smi-pre.txt
pgrep -af cove-replay-engine > …/pgrep-cove-pre.txt   (matches contain shell command literal)
pgrep -af ffmpeg > …/pgrep-ffmpeg-pre.txt             (matches contain shell command literal)
uptime > …/uptime-pre.txt

RUST_LOG=info,cove_replay_engine=debug node dist-validation/runner.js smoke
  > …/runner-stdout.txt 2> …/runner-stderr.txt

nvidia-smi > …/nvidia-smi-post.txt
pgrep -af cove-replay-engine > …/pgrep-cove-post.txt
pgrep -af ffmpeg > …/pgrep-ffmpeg-post.txt
kscreen-doctor -o > …/kscreen-doctor-post.txt
kscreen-doctor -j > …/kscreen-doctor-post.json
uptime > …/uptime-post.txt
cp -a validation-artifacts/smoke/2026-05-21T21-57-54-390Z .story/handovers/evidence/2026-05-21-t-021-mvp-smoke-rerun-24/smoke-evidence-tree
```
