# ISS-014 Evidence Collection Handover — 2026-05-23

## Repro Environment

- OS: Linux 6.18.32-1-lts, KDE Plasma 6, Wayland
- GPU: NVIDIA (NVENC available, probe succeeds)
- Display: 3840×2160
- PipeWire: SHM fallback active (DMA-BUF negotiation hard-fails)
- PW fourcc: XR24
- App: npm run dev (Electron + cove-replay-engine helper)
- Helper binary: target/debug/cove-replay-engine (rebuilt with T-031 instrumentation)
- Commit: 6371f46 (ISS-014 encode-boundary format diagnostics)

## Artifact Paths

- Save Replay artifact: `/home/sin/Videos/Cove Recordings/Cove_Gaming_2026-05-23_134733_511.mp4`
- Helper engine artifact: `/run/user/1000/cove-screen-recorder/exports/exp-1779558352913925351-0011.mp4`

## Verdict: INCONCLUSIVE

The ISS-014 encode-boundary instrumentation (T-031) targets the helper engine's NVENC push_frame path. The "Save Replay" feature routes through a separate Electron-side libx264 encode path that bypasses the helper engine entirely. The instrumentation cannot fire from Save Replay.

PW stream metadata from prior engine sessions partially confirms the NVENC-side hypothesis pattern (XR24 fourcc, SHM buffer, 4bpp stride), with no refuting signals. But the exact NVENC encode-boundary values (nvenc_buffer_fmt, shm_size) were not captured because the instrumentation never fired.

**Which code path produced the original ISS-014 corrupted artifact is undetermined.** This blocks definitive confirmation/refutation.

## Evidence Paths

All under `.story/handovers/evidence/2026-05-23-iss-014-evidence/`:

- `engine-log-excerpts.txt` — PW stream format, DMA-BUF fallback, NVENC probe, engine startup
- `pw-stream-ready.txt` — all PW stream ready lines (fourcc, resolution, session IDs)
- `export-lifecycle-excerpt.txt` — last 20 export lifecycle entries (all pre-rebuild)
- `ffprobe-artifact.json` — Save Replay artifact (libx264, 2560×1440, 2.1 Mbps)
- `ffprobe-helper-export.json` — helper engine artifact (NVENC, 3840×2160, 47.7 Mbps)
- `artifact-path.txt` — both artifact paths with sha256
- `verdict.md` — full verdict with diagnostic values and analysis

## Diagnostic Summary

| Signal | Value | Signature |
|---|---|---|
| PW fourcc | XR24 | CONFIRMING |
| Buffer type | Shm | CONFIRMING |
| DMA-BUF | hard-fail, SHM fallback | CONFIRMING |
| Stride (derived) | 4 bpp = 4000 | CONFIRMING |
| NVENC available | yes (probe succeeded) | neutral |
| NVENC buffer fmt | unknown (instrumentation didn't fire) | — |
| Refuting signals | none detected | — |

## Remediation Work Justified?

**Not yet.** The confirming signals from PW stream metadata are strong but indirect. Before committing to a fix:

1. Determine which code path produced the original ISS-014 corrupted artifact
2. If helper engine path → repro through that path to capture instrumentation output
3. If Electron/libx264 path → the NVENC hypothesis is misdirected; new investigation needed
